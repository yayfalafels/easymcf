# MCF Website Redirect - Feature tracker
>Review the guidelines before performing any actions including edits on the document

## 17 (closed) mcf website session

Add a global **MCF** nav control that runs MCF's Singpass login inside a local browser session EasyMCF owns, shows the resulting QR code and Singpass app link for approval on the user's phone, and hands the verified authenticated MCF session to apply automation once the account is confirmed.

## Contents

- [Tasks](#tasks)
- [Scope](#scope)
- [References](#references)
- [Design Probes](#design-probes)
  - [Probe script design](#probe-script-design)
- [Test strategy](#test-strategy)
- [Test cases](#test-cases)
- [Design](#design)
  - [Session coordinator and browser runner](#session-coordinator-and-browser-runner)
  - [Expiry and reauthentication](#expiry-and-reauthentication)
  - [Singpass browser automation seam](#singpass-browser-automation-seam)
  - [Data model](#data-model)
  - [API endpoints](#api-endpoints)
  - [Frontend](#frontend)
- [Other docs update locations](#other-docs-update-locations)
- [Edit locations](#edit-locations)
- [Implement](#implement)
- [Validate](#validate)
- [Tasks Details](#tasks-details)
- [Guideline](#guideline)

## Tasks

| id    | seq | status  | task                                                         |
| ----- | --- | ------- | ------------------------------------------------------------ |
| 17.01 | 01  | closed  | scope                                                        |
| 17.15 | 02  | closed  | verify live browser automation environment                   |
| 17.14 | 03  | closed  | probe design and evidence capture plan                       |
| 17.12 | 04  | closed  | probe Singpass redirect URL extraction                       |
| 17.13 | 05  | closed  | probe MCF account-identifying fields                         |
| 17.03 | 06  | closed  | test strategy                                                |
| 17.04 | 07  | closed  | test cases                                                   |
| 17.02 | 08  | closed  | design                                                       |
| 17.15 | 09  | closed  | other docs update locations                                  |
| 17.05 | 10  | closed  | edit locations                                               |
| 17.16 | 11  | closed  | data model seed data stubs                                   |
| 17.06 | 12  | closed  | implement session coordinator and attempt lifecycle          |
| 17.07 | 13  | closed  | implement browser runner login and QR extraction             |
| 17.08 | 14  | closed  | implement QR and link handoff frontend                       |
| 17.09 | 15  | closed  | implement account confirmation and connection status page    |
| 17.10 | 16  | closed  | implement disconnect and reauthentication handling           |
| 17.11 | 17  | closed  | validate session handoff                                     |
| 17.17 | 18  | closed  | authenticated browser for user                               |
| 17.IS | 19  | closed  | resolve issues                                               |

## Scope

Add a global **MCF** nav icon and pop-up that run MCF's Singpass login inside a local browser session EasyMCF owns and hand the resulting QR code and Singpass app link to the signed-in user for approval on their phone. The same browser that requested the login completes MCF's normal callback, and the resulting authenticated MCF session becomes the `mcf_session` row apply automation already reads.

- all steps other than the user's own Singpass approval can be executed by an autonomous claude ai agent with access to terminal using re-runnable idempotent scripts, and any check against the real `mycareersfuture.gov.sg` or Singpass origin stays human-gated per `ARCH-TEST-06` and `CLAUDE.md`'s boundary against unattended live-site access

In scope:

01. **connection attempt.** The user selects Connect MCF from the MCF nav page while signed in. The backend creates an attempt owned by that user and launches an isolated local Chromium context through Playwright. A repeated click while an attempt is already active reuses that same attempt.
02. **login navigation.** The runner opens `https://www.mycareersfuture.gov.sg/`, selects Login, follows the popup's Login with Singpass option, and follows the resulting redirect until Singpass displays a usable QR for the pending transaction. The runner keeps the same browser context for the rest of the attempt.
03. **QR and link handoff.** The runner extracts the actual Singpass app-launch URL when the page reliably exposes it, and decodes the displayed QR image otherwise. It validates the extracted destination against the known Singpass host and login path before showing it, using only the reference MCF's own page provides. The frontend shows the QR image, and on a device where a tappable link makes sense, the Open Singpass link, to the requesting user's own session only.
04. **phone approval.** The user scans the QR or taps the link and completes Singpass authentication, including any biometric or other challenge, in their own Singpass app. This step is always a human action performed on the user's own device, regardless of whether an agent is driving the rest of the attempt.
05. **callback verification.** The runner waits for the originating browser's own page to complete MCF's normal callback. It relies on MCF's and Singpass's own documented callback and page behavior to detect completion, leaving callback handling and authorization code redemption to MCF as the relying party. It confirms a stable authenticated MCF page before treating the attempt as connected.
06. **account confirmation.** The runner reads the account-identifying information MCF's authenticated page exposes. A first connection requires the user's explicit confirmation of the displayed account before it becomes usable. A later attempt compares against the previously confirmed identifier where MCF exposes a reliable one, and an unexpected account change is blocked and surfaced to the user for resolution.
07. **session record.** A verified and confirmed attempt exports the browser context's authenticated storage state to `.secrets/mcf_session_<user_id>.json` and its sessionStorage sidecar, then writes `mcf_session.cookie_ref` and `status`. This connection flow is the only path that establishes a usable MCF session.
08. **attempt lifecycle.** The attempt carries its own state, `starting`, `awaiting_approval`, `verifying`, `account_confirmation_required`, `connected`, `expired`, `cancelled`, `failed`, `interaction_required`, `reauthentication_required`, distinct from a search run or apply run's own state. If the Singpass page regenerates its QR, the frontend's image and link update together for the same attempt. A refresh retires the superseded browser context, so only the current attempt's approval can complete the active connection.
09. **ownership.** Every attempt, its QR, its link, and its status are authorized against the signed-in user per REQ-AUTH-06, and each browser context serves exactly one user.
10. **disconnect.** The user can disconnect a connected or in-progress attempt. Disconnect deletes easyMCF's retained session material for that user. When an authenticated browser context is still running, the owning thread clicks MCF's own Logout control before closing it; no fresh browser is opened only to log out. A connected attempt has already exported and closed its context, so disconnect then removes only EasyMCF's saved state.
11. **execution mode.** The runner is a background thread inside the existing local Flask process, holding one headless Chromium context through Playwright for the duration of one active attempt. The thread writes each state transition to the attempt's own row, so the status endpoint reads that row directly. Every Playwright wait carries its own timeout, so a stuck attempt reaches `failed` or `interaction_required` instead of leaving the thread blocked. The browser context itself closes once the attempt reaches `connected` or any other terminal state, its authenticated storage state already exported per item 07 by that point, so apply automation opens its own fresh context from that saved state when a run actually needs one, rather than a browser sitting open and idle between the two.
12. **document updates.** The **api design**, **data model**, **workflows**, and **requirements** documents are updated at the design stage, task 17.02, to describe the connection-attempt lifecycle and endpoints, and to replace REQ-APPLY-06's manual export-and-upload description with this feature's handoff as the way the existing `mcf_session` row is populated.

Out of scope:

01. Cloud or AWS-hosted browser execution, ECS or Fargate tasks, and any container orchestration. Cloud deployment stays out of scope for `010` per `CLAUDE.md`.
02. A remote desktop viewer, video or screen streaming, keyboard forwarding, or any VNC-style component. The headless QR-only flow needs none of them.
03. Embedding the live MCF website in an `<iframe>`, webview, proxy frame, or any other in-app browser surface.
04. Automating the Singpass authentication step itself. Biometric verification and app approval stay the user's own manual action, consistent with REQ-APPLY-06's exclusion of automating the login or MFA itself.
05. Cloud or encrypted-object-storage persistence of browser state. Local persistence reuses the existing `.secrets/mcf_session_<user_id>.json` mechanism and `cookie_ref` column `ARCH-BOT-03` already defines, described under Design, not a new storage mechanism of its own.
06. Any additional identity challenge beyond the reported QR-and-approval flow. An unsupported step reports `interaction_required` and stops there.
07. Live-site validation run unattended or by an agent on its own initiative. Any check against the real `mycareersfuture.gov.sg` or Singpass origin is human-gated per `ARCH-TEST-06` and `CLAUDE.md`'s boundary.
08. Multi-user cloud scaling of the browser runner. `010` runs one local backend process, and that same process serves each user's attempt directly.

**closure**

- **17.CK.01** clicking the **MCF** nav icon toggles the connection pop-up for the signed-in user's own state, and a repeated Connect action always reuses that user's active attempt
- **17.CK.02** the runner reaches the real Singpass QR page for a login it started, and extracts a validated app-launch URL or QR image using the exact reference MCF's page provides
- **17.CK.03** the page shows the current QR image, and where available a tappable Open Singpass link, scoped to the requesting user's own session only
- **17.CK.04** the user's phone approval of the QR or link results in an authenticated MCF session in the same browser context that requested it
- **17.CK.05** the authenticated account is presented to the user and requires explicit confirmation before the connection is usable, and an account change on a later attempt is blocked and surfaced for the user to resolve
- **17.CK.06** the connection is marked ready only after authenticated-session verification and account confirmation both succeed, and the `mcf_session` row apply automation reads reflects that state
- **17.CK.07** expiry, rejection, and cancellation each reach a safe terminal state that leaves the `mcf_session` row unchanged, and a fresh attempt is available afterward
- **17.CK.08** every attempt, QR, link, and browser-runner request is authorized against its owning user, and a request from a different user returns not found
- **17.CK.09** disconnect stops the owned browser and deletes easyMCF's retained session material for that user
- **17.CK.10** the feature's added surface is limited to the session coordinator, the browser runner, the QR and link handoff, and account confirmation, matching the components named in scope

## References

- **requirements** [010-01-requirements.md](../design/010-01-requirements.md): REQ-APPLY-06, REQ-AUTH-06, and the MCF session glossary entry
- **workflows** [010-workflows.md](../design/010-workflows.md): Workflow 8, MCF session establishment, and Decision 4, which this feature replaces with the handoff above
- **user interface** [010-user-interface.md](../design/010-user-interface.md): navigation structure and the session-status badge pattern this feature should follow
- **architecture** [010-architecture.md](../design/010-architecture.md): `ARCH-NET-01`, `ARCH-NET-03`, and `ARCH-TEST-06`
- **data model** [010-data-model.md](../design/010-data-model.md): the `mcf_session` table, its `status` values, and the per-user ownership pattern this feature keeps
- **api design** [010-api.md](../design/010-api.md): the `mcf_attempt` named endpoints, `POST /api/v1/mcf_session/open`, and the CAT-02 read hooks
- **mycareerfutures skill** [.claude/skills/mycareerfutures/SKILL.md](../../../../.claude/skills/mycareerfutures/SKILL.md): MCF site markup and session/cookie shape
- **playwright skill** [.claude/skills/playwright/SKILL.md](../../../../.claude/skills/playwright/SKILL.md): wait and locator patterns for the browser runner
- **apply automation tracker** [010.11-apply-automation.md](010.11-apply-automation.md): the consumer of the MCF session this feature establishes
- **oauth login tracker** [010.13-oauth-login.md](010.13-oauth-login.md): prior art for per-user ownership, sign-in status display, and human-gated live-tier checks
- **Singpass QR login** [singpass.gov.sg](https://www.singpass.gov.sg/main/individuals/): public description of scanning a QR to log in
- **Singpass app-launch from a mobile QR** [ask.gov.sg](https://ask.gov.sg/singpass/questions/clukzf6gp001cu36bkerx0rub): tapping the QR, iOS Open in Singpass, and Android app-link settings
- **Singpass authentication redirect** [docs.developer.singpass.gov.sg](https://docs.developer.singpass.gov.sg/docs/technical-specifications/integration-guide/2.-handling-the-redirect): authorization code, state, and relying-party callback processing that MCF itself performs
- **Singpass device requirements** [docs.developer.singpass.gov.sg](https://docs.developer.singpass.gov.sg/docs/getting-started/user-device-and-software-requirement): supported browser constraints and the face-verification step referenced in scope item 04

## Design Probes

these probe tasks settled two open design decisions, both closed against real live runs:

| id        | probe                                  |
| --------- | -------------------------------------- |
| 17.PB.01  | Singpass redirect URL extraction       |
| 17.PB.02  | MCF account-identifying fields         |

01. **Singpass redirect URL extraction.** Headless Chromium, launched with `channel="chromium"`, reaches the real Singpass QR page on its own, no bot-detection block observed. The page does not expose the app-launch URL as literal text anywhere in the DOM or the network trace. It renders the QR as an inline `<svg id="ndi-qr-canvas">`, decoded directly from that element with `cv2.QRCodeDetector`. MCF's own redirect into Singpass, `id.singpass.gov.sg/fapi/auth?client_id=...&request_uri=...`, is captured in the network trace but confirmed single-use, `PX-E0013 invalid_request_uri` when a second browser tried it after the first had already fetched it, so it stays diagnostic evidence rather than a handoff link. The decoded QR value is the one the runner hands to the user.
02. **MCF account-identifying fields.** The dashboard's own text exposes only a display name, `Hello <name>,`, not fit alone for scope item 06's account-confirmation check. Clicking the account-menu trigger, `<button data-testid="initial-circle-container">`, reveals the actual identifier directly underneath the name: the account's email address, present both in the opened menu and again on the profile page reached through its **Profile** link. The profile page also exposes extensive account content beyond identity, work history, skills, expected salary, and the uploaded resume's filename, none of it needed for account confirmation but confirming the depth of detail sitting behind that one menu click. `EASYMCF_ACCOUNT_EMAIL` matched against the extracted MCF email, and the probe's own interactive confirmation prompt, a live rehearsal of scope item 06's UX, completed end to end against a real Singpass login.

**probe prerequisites**

01. verify the local live-automation environment. `env/bin/python`'s Playwright already launches headless Chromium, confirmed locally with `playwright.chromium.launch(headless=True)` against version 151.0.7922.34, so task 17.14 can start directly against that already-installed Chromium.
02. design both probes as one script, reusing the `HEADLESS` and `MCF_MODE` env vars `easymcf/config.py` already defines, and follows `scripts/check_gcp_oauth.py`'s pattern of a small, git-tracked, rerunnable script rather than a pytest case, since the expected result is what the probe is discovering rather than a fixed assertion to check against. It captures a screenshot and the full page HTML at each navigation step under `.dev/logs/`, so a human can review what the runner actually saw without re-running the live site. It stops at extraction and observation, and leaves the Singpass approval itself, per out-of-scope item 04, to the human running task 17.13.
03. both probes need a human to trigger the run, per `CLAUDE.md`'s boundary against unattended live-site access. **Probe 17.PB.01** completes once the runner reaches the QR page. **Probe 17.PB.02** additionally needs the user's own real Singpass approval on their phone, the same manual action **Workflow 8** already asks for, so both probes are typically run together in one sitting to avoid a second live login.

### Probe script design

`scripts/probe_mcf_singpass.py` is the single git-tracked, rerunnable script both probes ran against, `scripts/check_gcp_oauth.py`'s pattern of a small standalone check rather than a pytest case, since the point was discovering unknown behavior, not asserting a known one. The file's own docstrings and comments are the source of truth for exact behavior; this section records the findings and reusable patterns worth carrying into the real implementation, not a restatement of the code.

**flow, as it actually ended up working**

01. reach `https://www.mycareersfuture.gov.sg/`, wait for its own client-rendered app to settle (`_wait_for_render()`), locate Login through `LOGIN_LOCATORS`'s ordered, explicit-priority candidates, since the real markup was unknown ahead of running this.
02. follow the click through to Singpass, launched with `channel="chromium"` rather than Playwright's headless default.
03. wait for the QR to render (`_wait_for_qr_render()`), decode it directly from the matched DOM element (`QR_ELEMENT_LOCATORS`, `_decode_qr_element()`), and hand it to the user both as a popup image (`_save_qr_popup()` / `_open_image_popup()`) and a decoded link, refreshing both on `QR_REFRESH_INTERVAL_MS` rather than one static capture.
04. once MCF's callback returns this same browser, wait for render again, print the dashboard's own text, then click the account-menu trigger (`ACCOUNT_MENU_TRIGGER`) and follow any revealed profile link (`PROFILE_LINK_LOCATORS`), scanning everything captured with `_find_email()`.
05. compare the found email against `EASYMCF_ACCOUNT_EMAIL` and rehearse the actual confirm/reject prompt with a real `input()`, not a simulated one.

**findings and patterns for the real implementation**

- **client-rendered pages lie about being ready.** Hit three times independently, MCF's home page, Singpass's QR page, and MCF's own post-approval page, all still show a loading skeleton at `domcontentloaded`. Every navigation this script makes is followed by a `networkidle` wait before reading anything (`_wait_for_render()`, folded into `_wait_for_qr_render()` too). The real browser runner needs the same discipline everywhere it navigates, not only at the two spots this probe happened to hit it.
- **an explicit-priority locator list beats a combined CSS selector.** `QR_ELEMENT_LOCATORS` exists because `"canvas, img[src^='data:image']"` matched two unrelated decorative icons before it ever found the real QR, DOM order deciding which alternative wins. `LOGIN_LOCATORS` and `SINGPASS_OPTION_LOCATORS` already used the safer pattern; `_first_match()` is the one shared implementation:
  ```python
  QR_ELEMENT_LOCATORS = [
      ("id=ndi-qr-canvas", lambda pg: pg.locator("#ndi-qr-canvas")),
      ("data-testid=ndi-qr-canvas", lambda pg: pg.locator("[data-testid='ndi-qr-canvas']")),
      ("canvas", lambda pg: pg.locator("canvas")),
      ("svg[role=img]", lambda pg: pg.locator("svg[role='img']")),
      ("img[data:image]", lambda pg: pg.locator("img[src^='data:image']")),
  ]
  ```
- **the real Singpass QR is an inline `<svg id="ndi-qr-canvas">`**, confirmed against a live page, never a `<canvas>` or a raster `data:` image. `_decode_qr_element()` still works for either fallback shape, since Playwright's element `.screenshot()` renders whatever is on screen regardless of element type.
- **`cv2.QRCodeDetector` fails a small, borderless QR regardless of source**, confirmed against a freshly `cv2`-generated one before ever touching Singpass. `_decode_qr_element()` upscales with nearest-neighbor interpolation and pads a white quiet zone before decoding.
- **MCF's own redirect into Singpass is single-use**, `id.singpass.gov.sg/fapi/auth?client_id=...&request_uri=...`, confirmed by `PX-E0013 invalid_request_uri` when a second browser opened it after the first had already fetched it. Capturing it without the original browser consuming it was tried four ways, a compiled regex matcher, a Python predicate, routing every request and filtering in Python, `service_workers="block"`, and none of them caught it: a confirmed Chromium/Playwright cross-origin renderer-swap limitation, not a bug in this script, so the on-page QR stays the only mechanism that actually works for handoff.
- **the dashboard alone shows only a display name.** The account-menu trigger, `<button data-testid="initial-circle-container">`, reveals the account's actual email directly underneath it, and its **Profile** link exposes far more account detail again, work history, skills, expected salary, resume filename. The real account-confirmation step needs to look there, not just read the landing page.
- **`stdout` is fully buffered once redirected to a file**, not line-buffered the way a terminal is, so a long-running, human-monitored script needs `sys.stdout.reconfigure(line_buffering=True)` or its progress, including a link the human is waiting to act on, never appears until the process exits.
- **`explorer.exe` from WSL2 opens a file in the OS's own viewer** when given a `wslpath -w`-converted path, but its own exit code is not a reliable success signal, a documented WSL interop quirk.
- **static analysis is cheap insurance against a live run that costs a real login.** `pyflakes`, added to `.dev/dev-env`'s `pyproject.toml`, caught a `NameError` on an undefined constant and, separately, an edit that had silently swallowed an `elif` branch, both before they could waste another real Singpass approval.

Evidence lands under `.dev/logs/<ts>-17.PB-probe/`, one screenshot, one HTML dump, and one visible-text dump per step, plus a `findings.json` summarizing what each step found and a `network.jsonl` of every captured request and response. The script never submits, applies, or clicks anything outside the Login-to-Singpass-and-back path, and it only ever writes what the rendered page shows, never a cookie or session value, to its own evidence files. Those same captured HTML dumps, the MCF home page, the Singpass QR page, the account menu, and the profile page, are the natural seed for a fixture corpus the real implementation's automated tests can run against instead of the live sites, the same role `ARCH-TEST-04`'s canned MCF HTML already plays for search and apply.

## Test Strategy

Seven layers, following the **test strategy doc**'s own `STRAT-*` framework and running in the same order `STRAT-TOOL-04` already establishes for every other functional milestone: each layer checks independently of the layers before it, so a claim at one layer gets a second, independent confirmation.

01. **self-report.** Case-config replay through the **api tester** and the **ui tester**, `STRAT-TOOL-01/02`, against a running instance, once task 17.02 names the actual connection-attempt endpoints. `tests/backend/cases/mcf_connection.json` and `tests/frontend/checks/mcf_connection_ui.json` extend the existing directories, the same replay mechanism every other milestone already uses.
02. **direct inspection.** `scripts/db_util.py` against `mcf_session` and whatever attempt/session-registry table task 17.02 adds, `STRAT-SILO-02`, confirming a write actually persisted, the `status` transition, the confirmed account identifier, independent of what the API response claimed.
03. **oracle.** hard-coded-expectation pytest calling the session-coordinator and attempt-lifecycle service directly, `STRAT-SILO-05`'s pattern for search and apply's own service layer, covering the full attempt state machine, every state in scope item 08's vocabulary and every legal transition between them, against a fixture browser.
04. **automation silo, a Singpass fixture seam.** `ARCH-BOT-02`'s `MCFBrowser` deliberately kept Singpass outside its own seam, because automating login was out of scope under the old design. This feature's whole point is bringing that flow into an automated seam, so it needs the equivalent split: a `fixture.py`/`live.py` pair task 17.02 names, gated by the same `MCF_MODE` `ARCH-RUN-08` already defines. The fixture corpus, `tests/fixtures/singpass/`, seeds directly from this tracker's own probe evidence, `.dev/logs/*-17.PB-probe/qr-singpass-qr.html`, `qr-account-menu.html`, and `qr-account-profile.html`, real markup this tracker's own probe runs already proved accurate. Route-intercepted per `ARCH-TEST-04`'s existing pattern, this lets extraction, decoding, and account-comparison logic run deterministically against a fixed reference, never a real rotating QR, per `STRAT-CASE-06`.
05. **mock end to end.** Playwright against the real backend and the same Singpass fixture corpus, `ARCH-TEST-04/09`'s existing pattern, confirming the frontend's QR image, its Open Singpass link, and its status indicator wire together correctly across the whole flow.
06. **live tier.** human-gated exactly as `ARCH-TEST-06` and this feature's own probes already were: a real Singpass approval and a real MCF account, triggered only by the user's own explicit action, never by an agent's initiative. This is the same live check the probes already performed, run again here as this feature's own closing validation.
07. **human validation.** `STRAT-HUMAN-01/02`, the last step, once every layer above passes: does the printed QR actually look right, does the nav entry read correctly, does the account-confirmation wording make sense to someone who isn't the person who wrote it.

Strategy rules specific to this feature:

01. **the probe's own findings become permanent regression cases**, `STRAT-CASE-04`. A render-wait must precede every read of MCF or Singpass page state: a fixture case captures a page mid-loading-skeleton and asserts the runner waits for it to settle before reading it. QR-element selection uses the explicit-priority locator list: a fixture case deliberately places a decorative icon ahead of the real QR element in DOM order and asserts the runner still picks the right one. The MCF-to-Singpass redirect capture stays a documented out-of-scope boundary, confirmed unworkable by the probe, so it needs no case of its own beyond that boundary itself.
02. **state-machine coverage**, `STRAT-CASE-02`. Every state in scope item 08's vocabulary, `starting` through `reauthentication_required`, needs a reachable fixture scenario and its legal transitions enumerated, the same treatment the lead status/stage machine already gets.
03. **ownership**, `STRAT-SILO-08` and `STRAT-CASE-08`. Every attempt, its QR, its link, and its status endpoint gets the tenancy-matrix treatment against a second seeded user, generated from the resource registry the same way milestone 13's own regression case already is, so a new resource added later fails until it is scoped.
04. **determinism**, `STRAT-CASE-06`. Singpass's real QR rotation, its bot-detection timing, and any wall-clock dependency are removed entirely from every tier but the live one; only a human-triggered live run ever sees a real, time-limited QR.

## Test Cases

Closure items 17.CK.01 through 17.CK.10 are the acceptance criteria this feature's own **Scope** section already states. Each row below names the layer that owns it; a full case-id scheme, `17.TC.*`, is assigned once task 17.02 settles the actual endpoint and table names it would reference.

| id       | scope-item | layer       | check                                                                 |
| -------- | ---------- | ----------- | --------------------------------------------------------------------- |
| 17.TC.01 | 17.CK.01   | oracle      | repeated Connect MCF click reuses the same active attempt             |
| 17.TC.02 | 17.CK.02   | automation  | fixture-driven runner reaches the QR page, extracts a validated URL   |
| 17.TC.03 | 17.CK.02   | automation  | named regression: priority locator picks the real QR over a decoy     |
| 17.TC.04 | 17.CK.02   | automation  | named regression: a render-wait precedes every page-state read        |
| 17.TC.05 | 17.CK.03   | mock e2e    | frontend shows the current QR and Open Singpass link, user-scoped     |
| 17.TC.06 | 17.CK.04   | live tier   | a real phone approval authenticates the requesting browser context    |
| 17.TC.07 | 17.CK.05   | oracle      | first connection requires confirmation; a later mismatch is blocked   |
| 17.TC.08 | 17.CK.06   | inspection  | `mcf_session` updates only after auth and confirmation both succeed   |
| 17.TC.09 | 17.CK.07   | oracle      | expiry, rejection, cancellation terminate with `mcf_session` intact   |
| 17.TC.10 | 17.CK.08   | oracle      | tenancy matrix: a second user's request for another's attempt is 404  |
| 17.TC.11 | 17.CK.09   | inspection  | disconnect stops the owned browser, deletes that user's own data      |
| 17.TC.12 | 17.CK.10   | self-report | the feature's added surface matches the components named in scope     |
| 17.TC.13 | scope 08   | oracle      | full state-machine transition table, every state reachable and legal  |
| 17.TC.14 | scope 09   | oracle      | knowing an attempt id alone is insufficient; every read is authorized |

**tools** — reproduce independently, once task 17.02 names the real commands:

```bash
<copy-pasteable command to re-run this check by hand, filled in when the endpoints and fixture corpus exist>
```

## Design

01. **requirements**: REQ-APPLY-06's manual export-and-upload description is replaced by this feature's own connection-attempt flow as the way `mcf_session` gets populated. REQ-AUTH-06 already covers per-user scoping as it stands.
02. **architecture**: `ARCH-NET-03` gains the Singpass origin as a second permitted outbound destination, the deviation below. `ARCH-BOT-02` gains a second, parallel `SingpassBrowser` seam alongside `MCFBrowser`, since Singpass moves from deliberately outside the automation seam to inside it.
03. **data model**: the new `mcf_attempt` table and the two new `mcf_session` columns below, `SCHEMA_VERSION` advancing by one from whatever milestone 09/10/11 land on.
04. **api design**: the `mcf_attempt` endpoints below, following the same `run_log` trigger-plus-generic-read split already established.
05. **workflows**: Workflow 8 is rewritten around this attempt lifecycle in place.
06. **user interface**: one MCF nav icon with a stoplight indicator and a global connection pop-up, replacing the earlier page-based design.

### Session coordinator and browser runner

The connection-attempt workflow runs entirely inside the existing local Flask backend process, using a background thread, the same choice `ARCH-RUN-02` already made for search and apply runs. This keeps the runner inside the same process as the rest of the backend, sharing its existing SQLite connection pattern directly. Each thread holds one headless Chromium context through Playwright, `channel="chromium"` per **17.PB.01**'s finding, scoped to one active attempt, and closes it once that attempt reaches a terminal state or is disconnected.

**attempt flow**, informed directly by the probe's own findings:

01. `POST /api/v1/mcf_attempt/start` creates the row (`starting`) and starts the thread, or returns the existing row if one is already in flight for that user, matching scope item 01's reuse requirement.
02. the thread opens `https://www.mycareersfuture.gov.sg/`, waits for the page's own render to settle before reading anything, the client-rendered-app finding **17.PB.01** hit twice, clicks Login, and follows it to Singpass, moving the row to `awaiting_approval` once a QR is visible.
03. it locates the QR through the explicit-priority locator list, `#ndi-qr-canvas` first, the same fix **17.PB.01** needed after a decorative icon matched ahead of the real QR under a combined CSS selector, and decodes it directly from that element's own bitmap.
04. it waits for the page's own navigation back to an `mycareersfuture.gov.sg` host, moving the row to `verifying`, then waits for that page's own render to settle again before reading it, the same finding a second and third time.
05. it clicks the account-menu trigger, `[data-testid="initial-circle-container"]`, since **17.PB.02** found the dashboard's own text exposes only a display name; the account holder's email sits directly underneath that click. The row moves to `account_confirmation_required` with the found email attached.
06. once the user confirms through the frontend, the row moves to `connected`, the browser exports its own authenticated storage state to `.secrets/mcf_session_<user_id>.json`, and the coordinator writes the existing `mcf_session` row, `status='valid'` and `cookie_ref` pointing to that file, the same fields apply automation already reads. Its own browser context then closes, having already handed off what it captured. A rejection, an unconfirmed mismatch, or a timeout at any step moves the row to `failed` or `interaction_required` instead, leaving `mcf_session` unchanged, per closure item 17.CK.07.

the QR value and the found email, while unconfirmed, live only in an in-memory registry the coordinator keeps, keyed by attempt id, the same short-lived, sensitive handling the reference spec's own notes call for. Only the confirmed outcome persists to disk: the account email once the user accepts it, and the exported storage state once the attempt reaches `connected`, both written to `mcf_session` itself, defined below.

**deviation** - `ARCH-NET-03`'s outbound allow list currently names only the MCF origin. This feature adds the Singpass origin the runner actually redirects to, `app.singpass.gov.sg` and any host observed alongside it, the same way milestone 13 added the Google endpoints for OAuth.

### Expiry and reauthentication

The attempt's own browser context closes once it reaches `connected`, its authenticated storage state already exported to `mcf_session.cookie_ref` by that point per scope item 07. Apply automation opens its own fresh context from that saved file and restores the cookies into it each time a run actually needs one. This is also what keeps a connected session intact across a backend restart: the file survives one, an in-memory-only browser would not have.

MCF's own session can still lapse on its own side at any later point, restored or not. The detection this feature relies on is the same one `REQ-APPLY-06` already establishes: an apply run that opens its restored context and lands on MCF's login page instead of the page it expected writes `mcf_session.status='expired'` and moves the owning attempt row to `reauthentication_required`, the vocabulary value scope item 08 already names for exactly this case. This reuses an existing trigger, one check with one place to get its timing right.

The frontend polls `mcf_attempt` and `mcf_session` together for the nav indicator and pop-up. The next poll after either row changes shows `reauthentication_required` directly. That state renders with Connect MCF available again, the same control a never-connected user sees, since `reauthentication_required` sits outside `ux_mcf_attempt_active` alongside `connected`, `expired`, `cancelled`, and `failed`.

### Singpass browser automation seam

`ARCH-BOT-02`'s `MCFBrowser` deliberately kept Singpass outside its own seam, since automating login was out of scope under the design this feature replaces.

`easymcf/automation/singpass_browser.py` gives Singpass the same fixture/live split `MCFBrowser` already has, `fixture.py` and `live.py` behind one interface, gated by the same `MCF_MODE` `ARCH-RUN-08` already defines. An automated run uses `fixture.py`, routed against the canned corpus below. A human who deliberately sets `MCF_MODE=live` runs the identical code path against the real `singpass.gov.sg` host, the same human-triggered live tier `ARCH-TEST-06` and this feature's own probes already established.

The fixture corpus, `tests/fixtures/singpass/`, is seeded directly from this tracker's own probe evidence, `qr-singpass-qr.html`, `qr-account-menu.html`, and `qr-account-profile.html`, real markup **17.PB.01** and **17.PB.02** already proved accurate.

### Data model

**`mcf_attempt`**, new table.

| column         | type    | notes                                                     |
| -------------- | ------- | --------------------------------------------------------- |
| `id`           | INTEGER | primary key                                                |
| `user_id`      | INTEGER | FK `user.id`                                               |
| `status`       | TEXT    | `CHECK` against the ten-value vocabulary, scope item 08    |
| `account_email`| TEXT    | nullable, the confirmed identifier once `connected`         |
| `error_code`   | TEXT    | nullable, set on `failed`/`interaction_required`            |
| `created_at`   | TEXT    | ISO-8601, per `ARCH-STO-05`'s clock convention              |
| `updated_at`   | TEXT    | ISO-8601, refreshed on every state change                   |

A partial unique index, `CREATE UNIQUE INDEX ux_mcf_attempt_active ON mcf_attempt(user_id) WHERE status IN ('starting','awaiting_approval','verifying','account_confirmation_required')`, mirrors `ARCH-RUN-03`'s own pattern for `run_log`: the database itself enforces at most one in-flight attempt per user, atomically, on the `INSERT` that starts a new one. The other six states, `connected`, `expired`, `cancelled`, `failed`, `interaction_required`, and `reauthentication_required`, fall outside the index, so a user can always start a fresh attempt once the previous one reaches any of them, and past attempts remain queryable history.

**`mcf_session`**, the complete table as this feature leaves it, five existing columns and two new ones.

| column                    | state    | notes                                                        |
| ------------------------- | -------- | ------------------------------------------------------------ |
| `id`                      | existing | PK                                                              |
| `user_id`                 | existing | unique FK to `user`, one row per user                           |
| `status`                  | existing | `valid`\|`expired`\|`missing`, written by this feature now     |
| `uploaded_at`             | existing | last confirmed-valid timestamp                                 |
| `cookie_ref`              | existing | exported storage-state filename, written by this feature now  |
| `confirmed_account_email` | new      | nullable, set on the first confirmed attempt                   |
| `confirmed_at`            | new      | nullable, ISO-8601 timestamp of that confirmation              |

`status` and `cookie_ref` are the same columns `ARCH-BOT-03` and milestone 13 already defined for the manual upload flow, now populated by this feature's own attempt flow instead of a pasted cookie. The confirmed identifier lives in its own two new columns since it must survive longer than any one attempt row does: a later attempt's `account_email` compares against `mcf_session.confirmed_account_email`, per scope item 06, and a mismatch is blocked and surfaced to the user for resolution, matching closure item 17.CK.05.

### API endpoints

| endpoint                                | class       | responsibility                                       |
| --------------------------------------- | ----------- | ---------------------------------------------------- |
| `POST /api/v1/mcf_attempt/start`        | named       | create or return the caller's in-flight attempt      |
| `GET /api/v1/mcf_attempt/search`        | CAT-02 hook | poll status, scoped to the caller, no QR value       |
| `GET /api/v1/mcf_attempt/{id}/qr`       | named       | current QR image, no-store, from the memory registry |
| `GET /api/v1/mcf_attempt/{id}/qr_link`  | named       | current decoded Singpass app link                    |
| `POST /api/v1/mcf_attempt/{id}/confirm` | named       | accept the displayed account, writes `mcf_session`   |
| `DELETE /api/v1/mcf_attempt/{id}`       | named       | cancel or disconnect, stops the owned browser        |

This follows `run_log`'s own trigger-endpoint-plus-generic-read split: a named endpoint starts the real work, a generic-shaped read reports on it, and the QR and confirm/cancel actions each get their own named endpoint because none of them is a plain field write. Every endpoint is authorized against the signed-in user per `REQ-AUTH-06`; an attempt id belonging to another user answers `404`, the same tenancy rule every other resource already follows, closure item 17.CK.08.

`reauthentication_required` reaches `mcf_attempt` through none of the named write endpoints above. Apply automation's service writes it directly through the internal call described under **Expiry and reauthentication**. `GET /api/v1/mcf_attempt/search` and `GET /api/v1/mcf_session/search` surface the result, so the frontend needs no separate endpoint.

### Frontend

The MCF nav icon initializes the shared poll and shows a red, amber, or green status dot. Clicking it toggles one global pop-up. The pop-up swaps content as the latest attempt advances through starting, QR approval, account confirmation, mismatch, failure, and connected states. There is no MCF route, status page, Session tab, or attempt-history view.

A user-closable X dismisses the modal at any point. The attempt itself keeps running regardless, since the coordinator's own background thread is independent of whether the modal happens to be open, which is exactly what makes reopening it from either entry point show that same in-progress state. The nav icon toggles that same pop-up open and closed. Once `status` reaches `connected`, the pop-up stays open on the connected state until the user dismisses it, matching the persistent user-session interaction.

`awaiting_approval` renders the QR from `GET .../qr` inside a modal whose dimensions follow the QR's own comfortable scanning size. The probe's own captured QR rendered natively at 160×160 CSS pixels, small enough for a person to read on screen while still risking an unreliable phone-camera scan at a normal scanning distance, the same class of problem `_decode_qr_element()` itself had to fix before this feature ever reached a frontend. The modal's QR display area upscales that native render comfortably larger, with the decoded link shown alongside it as a tappable Open Singpass control where the device makes that sensible. The browser runner re-reads and replaces both values every 15 seconds while awaiting approval, and the frontend's existing 3 second poll displays the current pair.

`account_confirmation_required` shows the found account within the same modal and blocks on the user's own confirm or reject action before `mcf_session` changes. Disconnect calls `DELETE /api/v1/mcf_attempt/{id}`, removes saved state, and returns the pop-up to missing.

## Other docs update locations

Task 17.15. The **oauth login tracker** stays as it is, delivered milestone trackers record what they built, the same precedent that milestone sets for the ones before it. The **release roadmap** names no milestone 17 entry to update.

| id | doc                     | section                       | change                                |
| -- | ----------------------- | ----------------------------- | ------------------------------------- |
| 01 | **requirements**        | REQ-APPLY-06                  | rewritten around the attempt flow        |
| 02 | **requirements**        | Glossary, MCF session         | describes automated establishment        |
| 03 | **architecture**        | `ARCH-NET-03`                 | Singpass origin added                    |
| 04 | **architecture**        | `ARCH-BOT-02`                 | `SingpassBrowser` seam added              |
| 05 | **architecture**        | `ARCH-BOT-03`                 | `cookie_ref` written automatically too   |
| 06 | **data model**          | `mcf_session`                 | two new columns                          |
| 07 | **data model**          | new entity                    | `mcf_attempt`, full table                |
| 08 | **data model**          | Schema versions               | version bump                             |
| 09 | **api design**          | new endpoints                 | `mcf_attempt` start/search/qr/confirm/delete |
| 10 | **api design**          | `mcf_session` section          | automated write path noted               |
| 11 | **workflows**           | Workflow 8                    | rewritten around the attempt flow        |
| 12 | **workflows**           | Decision 4                    | superseded by this feature's handoff     |
| 13 | **user interface**      | Navigation                    | MCF nav icon and status indicator        |
| 14 | **user interface**      | global pop-up                 | QR, account, Open, and Disconnect        |
| 15 | **frontend app**        | shared components             | service, icon, modal, and link directive |
| 16 | **apply automation tracker** | Scope, Design            | reads `cookie_ref`, writes reauthentication |

**requirements**: REQ-APPLY-06 is rewritten around this feature's own connection-attempt flow as the way `mcf_session` gets populated, in place of the manual export-and-upload description it currently gives. The glossary's MCF session entry follows the same change.

**architecture**: `ARCH-NET-03`'s outbound allow list gains the Singpass origin, the deviation this tracker's own Design section already names. `ARCH-BOT-02` gains `SingpassBrowser` as a second seam alongside `MCFBrowser`, fixture and live halves gated by the same `MCF_MODE`. `ARCH-BOT-03`'s note that the cookie payload is written only by the manual upload gains this feature's own attempt flow as a second, now primary, writer of the same file and column.

**data model**: `mcf_session` gains `confirmed_account_email` and `confirmed_at`. `mcf_attempt` is added as a new entity, its full column list and its partial unique index. The Schema versions table gains the row this bump gets once task 17.02's numbering is confirmed against whatever milestone 09 through 11 land on by then.

**api design**: the five `mcf_attempt` endpoints, `POST .../start`, `GET .../search`, `GET .../{id}/qr`, `POST .../{id}/confirm`, `DELETE .../{id}`, are added with their own `API-EP` numbers, following the `run_log` trigger-plus-generic-read shape. The existing `mcf_session` section gains a note that `status` and `cookie_ref` are now written by this attempt flow as well as the manual upload path it supersedes.

**workflows**: Workflow 8 is rewritten in place around the attempt flow, its QR/link handoff, account confirmation, and the reauthentication trigger, replacing the manual redirect-export-upload steps it currently describes. Decision 4, which the manual upload dialog answered, is superseded by this feature's own handoff, and this tracker's own References section already points here for that reason.

**user interface**: the nav gains one MCF icon with a stoplight indicator. Its global pop-up shows the QR, account confirmation, current session, Open, and Disconnect. There is no separate MCF page or Automation Session tab.

**frontend app**: `McfConnect` owns polling and actions. Shared `mcf-nav-icon`, `mcf-connect-modal`, and `mcf-link` directives expose the state and authenticated-link behavior without a feature route.

**apply automation tracker**: its own Design and Scope sections read `mcf_session.cookie_ref` as this feature now populates it, and its apply-run service is where the reauthentication trigger this tracker's Design describes actually gets written, an MCF-interaction landing on MCF's own login page unexpectedly, the same detection `REQ-APPLY-06` already establishes generally.

## Edit locations

Task 17.05. The order of the table is the order of the implement tasks, and each row names the task it belongs to. A file touched again by a later task is not repeated; the Implement step for that later task names it directly.

| id       | task  | path                                               | change                                 |
| -------- | ----- | -------------------------------------------------- | -------------------------------------- |
| 17.EL.01 | 17.PB | `scripts/probe_mcf_singpass.py`                    | probe script for 17.PB.01 and 17.PB.02 |
| 17.EL.02 | 17.16 | `easymcf/db/schema.sql`                            | `mcf_attempt`, `mcf_session` columns   |
| 17.EL.03 | 17.16 | `easymcf/__init__.py`                              | `SCHEMA_VERSION` bump                  |
| 17.EL.04 | 17.16 | `easymcf/tenancy.py`                               | `mcf_attempt` scope predicate          |
| 17.EL.05 | 17.16 | `scripts/gen_test_data.py`                         | `mcf_attempt`, `mcf_session` cols      |
| 17.EL.06 | 17.16 | `seed/*.sql`                                       | regenerated, new `17_mcf_attempt.sql`  |
| 17.EL.07 | 17.16 | `tests/backend/test_schema_accounts.py`            | new, active-index constraint tests     |
| 17.EL.08 | 17.16 | `tests/backend/test_seed_data.py`                  | new, two-branch seed oracles           |
| 17.EL.09 | 17.06 | `easymcf/services/mcf_connection.py`               | new, the attempt coordinator           |
| 17.EL.10 | 17.06 | `tests/backend/test_mcf_attempt.py`                | new, full state-machine oracle         |
| 17.EL.11 | 17.07 | `easymcf/automation/__init__.py`                   | new, empty package marker              |
| 17.EL.12 | 17.07 | `easymcf/automation/singpass_browser.py`           | new, interface, shared nav logic       |
| 17.EL.13 | 17.07 | `easymcf/automation/singpass_fixture.py`           | new, fixture implementation            |
| 17.EL.14 | 17.07 | `easymcf/automation/singpass_live.py`              | new, live implementation               |
| 17.EL.15 | 17.07 | `tests/fixtures/singpass/`                         | new, authored from probe evidence      |
| 17.EL.16 | 17.07 | `tests/backend/test_singpass_browser.py`           | new, automation-silo tests             |
| 17.EL.17 | 17.08 | `easymcf/api/mcf_attempt_routes.py`                | new, five named endpoints              |
| 17.EL.18 | 17.08 | `easymcf/api/resources.py`                         | register `mcf_attempt`, `mcf_session`  |
| 17.EL.19 | 17.08 | `easymcf/__init__.py`                              | register `mcf_attempt_routes.bp`       |
| 17.EL.20 | 17.08 | `frontend/app/core/api-client.service.js`          | `mcf_attempt` endpoint methods         |
| 17.EL.21 | 17.08 | `frontend/app/core/mcf-connect.service.js`         | new, poll and modal state              |
| 17.EL.22 | 17.08 | `frontend/app/shared/mcf-badge/`                   | new, nav-bar status badge              |
| 17.EL.23 | 17.08 | `frontend/app/shared/mcf-connect-modal/`           | new, QR and confirmation modal         |
| 17.EL.24 | 17.08 | `frontend/app/mcf/`                                | new, the nav-destination page          |
| 17.EL.25 | 17.08 | `frontend/app/shared/nav-bar/nav-bar.directive.js` | MCF nav link, badge                    |
| 17.EL.26 | 17.08 | `frontend/app/app.routes.js`                       | `/mcf` route                           |
| 17.EL.27 | 17.08 | `frontend/index.html`                              | script tags, modal mount               |
| 17.EL.28 | 17.08 | `frontend/app/app.css`                             | badge, modal-close, QR sizing          |
| 17.EL.29 | 17.08 | `tests/frontend/checks/mcf_attempt.json`           | new, ui_tester checks                  |
| 17.EL.30 | 17.08 | `tests/backend/test_tenancy.py`                    | ownership oracle entries               |
| 17.EL.31 | 17.09 | `tests/e2e/test_mcf_connection_flow.py`            | new, mock end to end                   |
| 17.EL.32 | 17.IS | `frontend/app/shared/mcf-badge/`                   | removed, replaced by `mcf-nav-icon`    |
| 17.EL.33 | 17.IS | `frontend/app/shared/mcf-nav-icon/`                | new, one icon plus stoplight dot       |
| 17.EL.34 | 17.IS | `frontend/app/mcf/`                                | removed, no separate page              |
| 17.EL.35 | 17.IS | `frontend/app/app.routes.js`                       | `/mcf` route removed                   |
| 17.EL.36 | 17.IS | `frontend/app/shared/nav-bar/nav-bar.directive.js` | one icon, no text link                 |
| 17.EL.37 | 17.IS | `frontend/index.html`                              | script tags updated                    |
| 17.EL.38 | 17.IS | `frontend/app/core/mcf-connect.service.js`         | open/start split, stoplight, refresh   |
| 17.EL.39 | 17.IS | `frontend/app/shared/mcf-connect-modal/`           | current state only, no history         |
| 17.EL.40 | 17.IS | `frontend/app/app.css`                             | stoplight dot styles                   |
| 17.EL.41 | 17.IS | `easymcf/automation/singpass_browser.py`           | render-wait timeout widened            |
| 17.EL.42 | 17.IS | `tests/conftest.py`                                | `MCF_MODE` scrubbed for test isolation |
| 17.EL.43 | 17.IS | `.env`                                             | `MCF_MODE=live`, local and gitignored  |
| 17.EL.44 | 17.IS | `tests/e2e/test_mcf_connection_flow.py`            | pop-up flow and timeouts               |
| 17.EL.45 | 17.IS | `tests/frontend/checks/mcf_attempt.json`           | icon/pop-up flow, wider timeouts       |
| 17.EL.46 | 17.IS | `frontend/app/{app.css,core,mcf-nav-icon}/`        | nav icon, indicator, pop-up toggle     |
| 17.EL.47 | 17.IS | `easymcf/{automation,services}/`                   | QR refresh during approval             |
| 17.EL.48 | 17.IS | `tests/{backend,e2e}/`                             | QR refresh and pop-up lifecycle checks |

### 17.16 data model seed data stubs

**17.EL.02** `easymcf/db/schema.sql`. Add the header line `-- schema_version 8 (feature 17) adds mcf_attempt and mcf_session.confirmed_account_email/confirmed_at.` and change the row to `INSERT INTO meta (schema_version) VALUES (8);`. Add `mcf_attempt` directly after `mcf_session`, and replace `mcf_session` with its two new columns.

```sql
CREATE TABLE mcf_session (
    id INTEGER PRIMARY KEY,
    user_id INTEGER NOT NULL UNIQUE REFERENCES user(id),
    status TEXT NOT NULL CHECK (status IN ('valid', 'expired', 'missing')),
    uploaded_at TEXT,
    cookie_ref TEXT,
    confirmed_account_email TEXT,
    confirmed_at TEXT
);

CREATE TABLE mcf_attempt (
    id INTEGER PRIMARY KEY,
    user_id INTEGER NOT NULL REFERENCES user(id),
    status TEXT NOT NULL CHECK (status IN (
        'starting', 'awaiting_approval', 'verifying', 'account_confirmation_required',
        'connected', 'expired', 'cancelled', 'failed', 'interaction_required', 'reauthentication_required'
    )),
    account_email TEXT,
    error_code TEXT,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL
);
CREATE INDEX idx_mcf_attempt_user_id ON mcf_attempt(user_id);
CREATE UNIQUE INDEX ux_mcf_attempt_active ON mcf_attempt(user_id)
    WHERE status IN ('starting', 'awaiting_approval', 'verifying', 'account_confirmation_required');
```

**17.EL.03** `easymcf/__init__.py`. `SCHEMA_VERSION = 8  # bump alongside easymcf/db/schema.sql (ARCH-STO-03)`.

**17.EL.04** `easymcf/tenancy.py`. Add `"mcf_attempt": "mcf_attempt.user_id = :uid",` to `SCOPES`, next to the existing `mcf_session` entry.

**17.EL.05** `scripts/gen_test_data.py`. Add `"mcf_attempt"` to `TABLES`, after `"mcf_session"`. Every seeded `mcf_session` starts `missing` with no `cookie_ref` because seed generation creates no browser-state credential. Account 1 retains `confirmed_account_email` and `confirmed_at` as prior identity-confirmation history. Add two `mcf_attempt` rows after it: account 1's historical `connected` attempt and account 2's `failed` attempt with `error_code='SingpassTimeoutError'`, giving both nullable-field branches without claiming a current authenticated session.

```python
for account in ACCOUNTS:
    previously_confirmed = account["id"] == 1
    sql["mcf_session"] += insert("mcf_session", {
        "id": account["id"], "user_id": account["id"],
        "status": "missing",
        "uploaded_at": None,
        "cookie_ref": None,
        "confirmed_account_email": account["email"] if previously_confirmed else None,
        "confirmed_at": f"{anchor} 12:00:00" if previously_confirmed else None,
    }, "no real cookie payload is ever seeded; a seeded row must not claim an authenticated session")

attempt_rows = [
    {"id": 1, "user_id": 1, "status": "connected", "account_email": ACCOUNTS[0]["email"], "error_code": None,
     "created_at": f"{anchor} 11:58:00", "updated_at": f"{anchor} 12:00:00"},
    {"id": 2, "user_id": 2, "status": "failed", "account_email": None, "error_code": "SingpassTimeoutError",
     "created_at": f"{anchor} 11:58:00", "updated_at": f"{anchor} 11:59:00"},
]
for row in attempt_rows:
    sql["mcf_attempt"] += insert("mcf_attempt", row, "synthetic connection-attempt history")
```

**17.EL.06** `seed/*.sql`. Regenerated by `17.EL.05`'s generator: `16_mcf_session.sql` carries the two new columns, and the new `17_mcf_attempt.sql` carries the two rows above.

**17.EL.07** `tests/backend/test_schema_accounts.py`. `test_mcf_attempt_status_is_constrained`, alongside the existing account/session tests, confirms the `CHECK` rejects an unlisted status. `test_mcf_attempt_active_is_unique_per_user`, matching the existing `test_running_run_is_unique_per_user_and_type` for `run_log`, confirms `ux_mcf_attempt_active` blocks a second active-status row for the same user regardless of which active status it names, leaves another user's own active attempt unaffected, and never blocks a terminal status, even repeated.

**17.EL.08** `tests/backend/test_seed_data.py`. `test_two_users_with_distinct_data` gains an `mcf_attempt` row count. `test_seeded_mcf_connection_covers_both_branches`, new, reads both seeded rows directly. User 1's `mcf_session` is `missing` with no `cookie_ref`, its confirmed identity is retained, and its historical `mcf_attempt` is `connected`. User 2's `mcf_session` is `missing` with every confirmation column `NULL`, and its `mcf_attempt` is `failed` with `error_code` set.

### 17.06 session coordinator and attempt lifecycle

**17.EL.09** `easymcf/services/mcf_connection.py`, new. Owns the state machine and the in-memory registry the **Session coordinator and browser runner** design section describes. It never drives Playwright itself; `singpass_browser.py` does, behind the `SingpassBrowser` interface 17.07 builds.

Implementation surfaced a real defect the design's own event-free sketch had not accounted for: Playwright's sync API is thread-bound, so a `confirm()`/`cancel()` call from a Flask request thread cannot touch a browser object a different thread created — `17.IS.04` below. The coordinator's actual shape has the background thread own its `SingpassBrowser` instance for the attempt's entire life, including the wait for the user's own confirm/reject decision, communicating with `confirm()`/`cancel()` through a small per-attempt control object (a `threading.Event` plus a decision flag) instead of handing the browser across threads:

```python
def _run_attempt(config: Config, attempt_id: int, uid: int) -> None:
    db = get_connection(config.db_path)
    browser = get_browser(config)
    control = {"event": threading.Event(), "decision": None, "cancel_requested": False}
    with _LOCK:
        _REGISTRY[attempt_id] = {"qr_bytes": None, "qr_link": None, "control": control}

    def _checkpoint() -> None:
        if control["cancel_requested"]:
            raise _Cancelled()

    try:
        browser.open_qr_login()
        _checkpoint()
        qr_bytes, qr_link = browser.read_qr()
        with _LOCK:
            _REGISTRY[attempt_id]["qr_bytes"] = qr_bytes
            _REGISTRY[attempt_id]["qr_link"] = qr_link
        _set_status(db, attempt_id, "awaiting_approval")
        _checkpoint()

        try:
            browser.wait_for_authenticated()
        except PlaywrightTimeoutError:
            browser.close()
            if not control["cancel_requested"]:
                _set_status(db, attempt_id, "expired")  # 17.IS.02
            return
        _checkpoint()
        _set_status(db, attempt_id, "verifying")

        email = browser.read_account_email()
        _checkpoint()
        mismatch = _check_account_match(db, uid, email)
        if mismatch:
            browser.close()
            _set_status(db, attempt_id, "interaction_required", account_email=email, error_code=mismatch)
            return
        _set_status(db, attempt_id, "account_confirmation_required", account_email=email)

        control["event"].wait(timeout=CONFIRMATION_WAIT_S)
        if control["decision"] is True and not control["cancel_requested"]:
            cookie_ref = browser.export_storage_state(uid)
            browser.close()
            _write_mcf_session(db, uid, cookie_ref, email)
            _set_status(db, attempt_id, "connected")
        else:
            browser.close()
            if not control["cancel_requested"]:
                _set_status(db, attempt_id, "cancelled")
    except _Cancelled:
        browser.close()
    except Exception as exc:  # noqa: BLE001 — every failure mode lands in error_code, not a crashed thread
        try:
            browser.close()
        except Exception:  # noqa: BLE001
            pass
        if not control["cancel_requested"]:
            _set_status(db, attempt_id, "failed", error_code=type(exc).__name__)
    finally:
        with _LOCK:
            _REGISTRY.pop(attempt_id, None)
        db.close()
```

`confirm()` and `cancel()` never touch `browser` directly; `confirm()` sets the decision and the event, then bounded-polls the row for up to `POLL_FOR_TERMINAL_S` so the common case still answers synchronously with the final `connected` row, falling back to whatever the row currently holds — the frontend's own poll picks up a later transition either way. `cancel()` marks the row `cancelled` immediately (the user-visible effect) and sets `cancel_requested`; a still-running thread notices at its next checkpoint and closes its own browser without overwriting that status.

`mark_reauthentication_required(db, user_id)` is the one function milestone 11's apply-run service calls directly, an internal call rather than a routed endpoint: it sets `mcf_session.status='expired'` and moves that user's most recent `mcf_attempt` row to `reauthentication_required`.

**17.EL.10** `tests/backend/test_mcf_attempt.py`, new. Nine direct-inspection oracles against a stubbed `SingpassBrowser` (a plain Python fake, not Playwright — the automation seam itself is 17.07's own tier): active-row reuse, the full happy path through `confirm(accept=True)` writing `mcf_session`, `confirm(accept=False)` leaving it untouched, an account mismatch landing on `interaction_required` and blocking `confirm`, an approval timeout landing on `expired` rather than `failed`, an unexpected error landing on `failed` with its type as `error_code`, disconnect clearing `mcf_session` and removing the cookie file, `mark_reauthentication_required`'s two writes, and the tenancy check that a second user's attempt id answers not-found. Two of the nine originally failed against the real seeded data from task 17.16 (user 1 already carries a confirmed `mcf_session` and a `connected` history row) until reassigned to user 2, whose seed leaves `confirmed_account_email` `NULL` — a test-fixture mistake, not a coordinator defect.

### 17.07 Singpass browser automation seam

**17.EL.12** `easymcf/automation/singpass_browser.py`, new. The `SingpassBrowser` interface, the locator lists and decode pipeline lifted from `scripts/probe_mcf_singpass.py` (`QR_ELEMENT_LOCATORS`, `_wait_for_render()`/`_wait_for_qr_render()`, `_decode_qr_element()`), and `BaseSingpassBrowser`, a shared Playwright lifecycle both `fixture.py` and `live.py` extend: `open_qr_login()` navigates and follows Login through up to `MAX_SINGPASS_OPTION_HOPS` "choose Singpass" hops, `read_qr()` returns both the QR's own image bytes and its decoded link (the coordinator needs both — the served image and the frontend's Open Singpass control), `wait_for_authenticated()` waits for the same page's own navigation back to an `mycareersfuture.gov.sg` host, `read_account_email()` clicks `ACCOUNT_MENU_TRIGGER` and follows the profile link per **17.PB.02**, and `export_storage_state()` writes `.secrets/mcf_session_<user_id>.json`, the file `ARCH-BOT-03` already names. `get_browser(config)` selects `LiveSingpassBrowser`/`FixtureSingpassBrowser` by `config.mcf_mode`, never reading the environment itself, since the coordinator's background thread has no Flask request context to read `current_app.config` from.

**17.EL.13** `easymcf/automation/singpass_fixture.py`, new. `FixtureSingpassBrowser` overrides one hook, `_setup_routing()`, registering `page.route("**/*", handler)` before the first navigation and fulfilling four known URLs from `tests/fixtures/singpass/`; anything else answers an empty `204`, so an automated run never resolves a real hostname regardless of what a page happens to reference.

**17.EL.14** `easymcf/automation/singpass_live.py`, new. `LiveSingpassBrowser` adds no override at all — the identical navigation code real or fixture, only the bytes differ.

**17.EL.15** `tests/fixtures/singpass/`, new: `mcf_home.html`, `qr.html`, `dashboard.html`, `profile.html`. Authored fresh rather than the real captured snapshots themselves, deliberately minimal, each carrying only the real selector attributes `scripts/probe_mcf_singpass.py` already confirmed (`id="ndi-qr-canvas"`, `data-testid="initial-circle-container"`, the profile page's own email text) — the real captures are 130 to 180 KB of production markup with dozens of subresource requests a route-everything fixture would otherwise need to field one by one. `qr.html` carries a real `cv2.QRCodeEncoder`-generated QR as a data-URI `<img>`, decoding through the identical pipeline a live SVG screenshot would, and a client-side `setTimeout` navigates back to `dashboard.html` after 8 seconds, standing in for the phone-approval redirect Singpass's own page performs once scanned.

**17.EL.16** `tests/backend/test_singpass_browser.py`, new. Five tests: no real hostname resolved (every observed request stays within the fixture's own two host names), the QR page reached and its link decoded to the seeded reference, the priority-locator regression (17.TC.03), the render-wait sequence reaching a correct account email end to end (17.TC.04), and the exported storage-state file's name and `0600` permissions.

### 17.08 QR and link handoff frontend

**17.EL.17** `easymcf/api/mcf_attempt_routes.py`, new. A dedicated blueprint, the shape `easymcf/auth/routes.py` already established for a cohesive endpoint group: `POST /start`, `GET /<id>/qr` (the raw PNG, `Cache-Control: no-store`), `GET /<id>/qr_link` (JSON, a fifth endpoint the original four-endpoint plan had not separated out — the frontend's Open Singpass control needs the decoded link independently of the image bytes an `<img src>` can carry), `POST /<id>/confirm`, `DELETE /<id>`. `qr`/`qr_link` both call `tenancy.require_visible(...)` before reading the registry, closing a gap the original sketch left open: an attempt id alone, with no ownership check, would have let any signed-in user read another user's in-flight QR or decoded link.

**17.EL.18** `easymcf/api/resources.py`. Registers `mcf_attempt` read-only, and separately, `mcf_session` — `17.IS.03`: `mcf_session` had no read endpoint at all before this feature, only internal writers (`auth/accounts.py`, `auth/google.py`, now `mcf_connection.py`), discovered when the frontend's own status page returned `unknown table or unsupported operation`.

**17.EL.19** `easymcf/__init__.py`. `app.register_blueprint(mcf_attempt_routes.bp)` alongside the existing `auth_routes.bp` line.

**17.EL.20** `frontend/app/core/api-client.service.js`. Five named methods added alongside the file's existing `createManualLead`-style entries: `startMcfAttempt`, `mcfAttemptQrUrl` (a URL builder for `<img ng-src>`, not an `$http` call), `mcfAttemptQrLink`, `confirmMcfAttempt`, `cancelMcfAttempt`.

**17.EL.21** `frontend/app/core/mcf-connect.service.js`, new. The `McfConnect` factory: a singleton `state` object plus `ensurePolling()`/`open()`/`close()`/`confirm()`/`disconnect()`, the exact shape `OfferDialog`/`ConfirmDialog` already establish in this codebase — the design's own "one implementation with two entry points" language pointed at a `010-user-interface.md` Session tab that, on inspection, was never actually built in `frontend/`; this factory is that pattern's first real implementation, not a reuse of existing frontend code. `$interval`-driven, 3 second poll of `mcf_attempt`/`mcf_session` search.

**17.EL.22** `frontend/app/shared/mcf-badge/`, new. `mcfBadge`, mounted in `nav-bar` next to `user-menu`, showing `mcf_session.status` and opening the connect modal — the frontend's own "any page" entry point, since `nav-bar` is the one directive present on every guarded route.

**17.EL.23** `frontend/app/shared/mcf-connect-modal/`, new. `mcfConnectModal`, mounted once in `index.html` like `offer-modal`/`confirm-modal`: an `awaiting_approval` block with the QR (`320px`, upscaled well past its native size, `image-rendering: pixelated` to keep the modules crisp) and an Open Singpass link, an `account_confirmation_required` block, an `interaction_required` mismatch block, and a `failed`/`expired` block with a retry action. A user-closable `×` (`.modal-close`, new to `app.css` — the first modal in this codebase with one, `confirm-modal`/`offer-modal` use only their own Cancel buttons) dismisses it at any point without touching the running attempt; the modal closes itself once `status` reaches `connected`.

**17.EL.24** `frontend/app/mcf/`, new: `mcf.controller.js`, `mcf.html`. The nav-destination page: current status, Connect/Disconnect, and the full attempt history table.

**17.EL.25** `frontend/app/shared/nav-bar/nav-bar.directive.js`. Adds the `/mcf` nav link and mounts `<mcf-badge>` — the actual file the original plan's `frontend/app/shared/nav.js` reference meant; this codebase keeps navigation inside the `nav-bar` directive, there is no separate `nav.js`.

**17.EL.26**/**17.EL.27**/**17.EL.28** `frontend/app/app.routes.js` (the guarded `/mcf` route), `frontend/index.html` (new script tags, `<mcf-connect-modal>` mounted), `frontend/app/app.css` (`.modal-close`, `.mcf-qr`, `.mcf-badge`).

**17.EL.29** `tests/frontend/checks/mcf_attempt.json`, new. Three `ui_tester` checks against a real spawned app and real fixture-mode browser automation (not the mocked-API tier): an already-connected user sees `valid` and a disconnect control, a never-connected user sees Connect MCF, and the full connect-through-confirm flow reaches the QR, confirms, and shows `valid` with the modal closed. The first version of the third check split QR-reach and confirm into two separate checks sharing the one server `ui_tester.py --case` spawns for a whole file; the leftover unconfirmed attempt from the first check made the second start return that same stale row instead of a fresh one, so the two were merged into one self-contained check.

**17.EL.30** `tests/backend/test_tenancy.py`. Not in the original plan: this file auto-discovers every table registered in `easymcf.tenancy.SCOPES` and asserts a per-table ownership oracle exists for it, so registering `mcf_attempt`/`mcf_session` (17.EL.18) surfaced four failures the moment the full suite ran — `OWNER_SQL` needed one row per table, the same one-line pattern every other scoped table already uses.

### 17.09 account confirmation and connection status page

Built as part of 17.06's own `mcf_connection.py` (`_check_account_match`, called from `_run_attempt` right after `read_account_email()`) and 17.08's own modal/status-page frontend — the design's task split was by concern, not by which file changes, and these two pieces turned out tightly enough coupled to the coordinator and the modal that splitting their code across a separate edit would have meant editing the same functions twice. The task's own edit location is the validation that exercises them end to end:

**17.EL.31** `tests/e2e/test_mcf_connection_flow.py`, new. Full stack, `ARCH-TEST-04`'s tier, one layer over: real AngularJS, real Flask, `MCF_MODE=fixture` routing every Singpass/MCF request to `tests/fixtures/singpass/`. Two tests: the connect-confirm happy path asserting `mcf_session.status='valid'`, `cookie_ref`, and `confirmed_account_email` all written correctly and the modal/badge reflecting it, and the account-mismatch scenario, seeding a `confirmed_account_email` the fixture's own found email does not match, asserting the attempt lands on `interaction_required` and `mcf_session` stays untouched.

### 17.10 disconnect and reauthentication handling

Also built inside 17.06's own `mcf_connection.py` (`cancel()`, `mark_reauthentication_required()`) rather than as a separate edit — `cancel()` handles both the in-flight-cancel and the connected-disconnect cases in one function, since both need the same tenancy check and the same control-object signal to a still-owning background thread (17.IS.04). The frontend needed no dedicated edit either: `mcf.html`'s Connect/Disconnect buttons already key off `mcf_session.status !== 'valid'`, which is true for `reauthentication_required` exactly as it is for `missing`, so the "same control a never-connected user sees" the design calls for falls out of the existing conditional with no new markup. Verified directly: calling `mark_reauthentication_required(db, 1)` against the seeded `connected` user and loading `/mcf` as that user showed `status: expired`, the Connect MCF button, no Disconnect button, and a `MCF: EXPIRED` badge, with no code change needed beyond 17.06's own write.

A known limitation, not fixed in this pass: `cancel()`'s checkpoints run between major awaited steps, but `wait_for_authenticated()` itself blocks inside a single Playwright call for up to 15 minutes. A cancel during that specific window updates the database correctly and immediately, but the background thread and its Chromium instance keep running until that wait itself returns, either through an actual late approval or Playwright's own timeout, rather than closing immediately. The row is never wrong; the browser resource lingers.

### 17.IS.05 to 17.IS.11 nav icon and pop-up-only redesign

**17.EL.32**/**17.EL.33** Deleted `frontend/app/shared/mcf-badge/`; added `frontend/app/shared/mcf-nav-icon/mcf-nav-icon.directive.js`, one element covering both jobs the badge and the old text link split between them.

```javascript
angular.module('easymcfApp').directive('mcfNavIcon', ['McfConnect', function (McfConnect) {
  return {
    restrict: 'E', scope: {},
    link: function (scope) { scope.connect = McfConnect; McfConnect.ensurePolling(); },
    template:
      '<button type="button" class="mcf-nav-icon" ng-click="connect.openPopup()" data-testid="mcf-nav-icon" aria-label="MCF connection status">' +
      '<svg viewBox="0 0 24 24" width="20" height="20" aria-hidden="true">' +
      '<circle cx="10" cy="10" r="6.2" fill="none" stroke="#7a1f3d" stroke-width="2.4"></circle>' +
      '<line x1="14.6" y1="14.6" x2="20.5" y2="20.5" stroke="#7a1f3d" stroke-width="2.4" stroke-linecap="round"></line>' +
      '</svg>' +
      '<span class="mcf-status-dot mcf-status-{{ connect.statusLight() }}" data-testid="mcf-status-dot"></span>' +
      '</button>'
  };
}]);
```

**17.EL.34**/**17.EL.35** Deleted `frontend/app/mcf/` (`mcf.controller.js`, `mcf.html`) and its `/mcf` route in `app.routes.js`, per `17.IS.07`.

**17.EL.36**/**17.EL.37** `nav-bar.directive.js` mounts `<mcf-nav-icon>` in place of the old `<a href="/mcf">`/`<mcf-badge>` pair; `index.html` drops the deleted files' script tags and adds the new ones.

**17.EL.38** `mcf-connect.service.js`. `open()` split into `openPopup()` (just shows the pop-up) and `startAttempt()` (the actual `POST /start`, now a deliberate in-pop-up action rather than firing automatically on open, `17.IS.09`'s own login-disable guarantee depends on this split existing at all). Added `isActive(attempt)` and `statusLight()` (`17.IS.10`). `confirm()` now calls `refresh()` after a successful accept, closing a real staleness gap the redesign's own validation surfaced: `state.session` previously only updated on the next 3 second poll tick, so the pop-up could still offer Connect MCF for up to 3 seconds after a connection had already gone through.

**17.EL.39** `mcf-connect-modal.directive.js`. Added the `mcf-current-state` block (`17.IS.08`/`.09`): renders only when no attempt is active and the last one isn't an undismissed mismatch, showing `session.confirmed_account_email` plus Disconnect when valid, or a brief one-line reason (expired QR, failed attempt) plus Connect MCF otherwise. The QR/confirm/mismatch blocks from `17.08` are unchanged.

**17.EL.40** `app.css`. `.mcf-nav-icon` (the button chrome), `.mcf-status-dot`/`.mcf-status-{green,amber,red}` (the stoplight overlay), replacing the deleted `.mcf-badge` rule.

**17.EL.41** `easymcf/automation/singpass_browser.py`. `QR_RENDER_TIMEOUT_MS` widened from 20000 to 45000 — a production robustness fix `17.IS.07`'s own validation surfaced, not just a test-timeout tuning, since a busy host's real Chromium launch and navigation genuinely took 40 seconds or more.

**17.EL.42**/**17.EL.43** `tests/conftest.py` scrubs `MCF_MODE` from `os.environ` alongside the other `.env`-sourced keys it already removes; `.env` itself carries `MCF_MODE=live` (`17.IS.11`).

**17.EL.44**/**17.EL.45** `tests/e2e/test_mcf_connection_flow.py` and `tests/frontend/checks/mcf_attempt.json` updated for the icon-plus-pop-up interaction pattern (click the icon, not a page link) and widened timeouts, per `17.IS.07`'s own diagnostic trail.

## Implement

### 1. Probe script, 17.PB.01

edit locations: `17.EL.01`

`scripts/probe_mcf_singpass.py` implements the **Probe script design** steps 01 to 03: home navigation, the Login-locator search, following the click to a new tab or the same tab, up to `MAX_SINGPASS_OPTION_HOPS` intermediate "choose Singpass" clicks, and QR-reference extraction from the DOM or the captured network trace, writing evidence under `.dev/logs/<ts>-17.PB-probe/`. `--wait-for-approval` additionally implements step 04 for 17.PB.02. `env/bin/python -m py_compile scripts/probe_mcf_singpass.py` passes, the script's refusal path was confirmed with the default `MCF_MODE=fixture`, no network reached, and the locator-matching and QR-extraction logic were confirmed offline against synthetic HTML through `sync_playwright()` with no MCF or Singpass host contacted.

A first live run, `.dev/logs/20260922-145426-17.PB-probe/`, reached the real `login.id.singpass.gov.sg/main` page headless in one hop through a "Choose a login method" option, so headless Chromium is not blocked this far. `qr_reference_found` came back `false`: the 03-singpass-qr screenshot shows Singpass's own loading spinner in place of the QR, and `network.jsonl` ends mid-sequence at `id.singpass.gov.sg/csrf-token`, right after the bot-detection bundles, one step upstream of whatever request actually fetches the QR. The fix is `_wait_for_qr_render()`, an explicit wait for the network to go idle and then for a `canvas` or a data-URI `img` to become visible, called before the `03-singpass-qr` snapshot, rather than a fixed sleep, per the **playwright skill**.

The same evidence held `https://id.singpass.gov.sg/fapi/auth?client_id=...&request_uri=urn:ietf:params:oauth:request_uri:...`, MCF's own redirect into Singpass, carrying a client id and a PAR request-uri reference tied to that MCF session, available the instant the redirect fires with no rendering wait. `_extract_mcf_redirect()` captures it as `mcf_singpass_redirect_url`. Verified offline against that real captured line before rerunning live.

A second live run confirmed the QR-render fix works: `.dev/logs/20260922-150353-17.PB-probe/03-singpass-qr.png` shows a fully rendered QR this time, so `_wait_for_qr_render()` closes the timing gap the first run hit. `qr_reference_found` still came back `false` even so, because the QR renders as pixels with no literal `swk_qr_ref`-style URL anywhere in the DOM or the network trace to read, so extracting it needs decoding the rendered image, not text extraction, exactly Scope item 03's already-anticipated fallback, now confirmed as the actual required path rather than a rare case.

The user then manually opened the captured `mcf_singpass_redirect_url` from that same run in a separate browser, and Singpass answered `PX-E0013 invalid_request_uri`, "does not match the client associated with the referenced request URI." The first, headless browser had already consumed that `request_uri` reaching its own QR page, and PAR's `request_uri` is single-use and session-bound. This left one question open: is the URL unfit for cross-device handoff on principle, or did it simply arrive already spent because this browser used it first? `--capture-only` tests that directly. It registers `context.route()` against `SINGPASS_AUTH_REQUEST_RE` before the Login click, aborts the matching request the instant it fires, so this browser never fetches it at all, and hands the still-unconsumed URL to the human as its first and only consumer. It is mutually exclusive with `--wait-for-approval`, since capture-only deliberately never reaches the QR page. Compiled clean, the refusal path and the new flag guard both verified offline; the live capture-only run and the human's own attempt to open that fresh URL are next.

Decoding the on-page QR itself remains the fallback either way. `pyzbar` and `cv2` are both absent from `env` today, a new dependency `ARCH-RUN-05`'s burden-of-proof gate applies to, so that addition waits on a result before it's worth confirming.

`--capture-only` alone only proves whether the URL survives a second consumer at all, not whether that second consumer's approval reaches back to the original browser, the actual thing the feature needs, so `--wait-for-approval` now runs both branches in one pass: branch 1 captures the redirect without consuming it and retry-checks the original browser's own MCF cookies and Login control for a change, and branch 2, only reached if branch 1 times out, falls back to letting that same browser complete a real Login click through to the on-page QR. `_reach_qr_page()` factors the shared click-hop-render-snapshot sequence so both the no-flag default path and branch 2 call the same code under different `findings` key prefixes. Compiled clean, both guard rails re-verified offline, and the extraction helpers re-checked against the real captured redirect line and synthetic HTML with no MCF or Singpass host contacted.

A `--wait-for-approval` run hit `[FAIL] no Login control found on the MCF home page`. `.dev/logs/20260922-151622-17.PB-probe/01-mcf-home.png` is blank, and the HTML dump has literal `"Loading"` text: the same `domcontentloaded`-fires-before-render gap the QR page already hit once, now on MCF's own client-rendered home page. `_wait_for_render()` adds the same `networkidle` wait `_wait_for_qr_render()` already established, called after every `goto(MCF_HOME, ...)`: the initial navigation, `_check_authenticated()`'s reload, and branch 2's fallback reload. Compiled clean, refusal path re-verified.

A run without `--wait-for-approval` reached the real QR page and reported the redirect URL as captured, but the URL was already spent, this browser had fetched it for real to get there, and the run's own print statement never even showed the value, only a generic PASS line. Capture-without-consuming was meant to be the point of this whole script, not something only `--wait-for-approval` remembers to do, so the plain unflagged path that fully navigated through to the real QR page is gone. `main()` now defaults `capture_only` to `True` whenever `--wait-for-approval` is not set, so the redirect is captured and handed off, never fetched for real, unless `--wait-for-approval`'s own branch 2 fallback deliberately chooses to spend it. Compiled clean, both guard rails re-verified offline.

Both branches already stop once the original browser shows authentication, `_check_authenticated()` for branch 1 and `wait_for_url()` for branch 2, and both already call `_snapshot()` at that point, but `_snapshot()` only wrote a screenshot and the full HTML, not the visible page text the **Probe script design**'s step 05 also called for, 17.PB.02's actual raw material. `_snapshot()` now writes a `.txt` of `page.inner_text("body")` alongside the screenshot and HTML for every step, still with no field name assumed in advance, confirmed offline against synthetic HTML with a name and a masked NRIC in the body.

A `--wait-for-approval` run answered `[FAIL] never intercepted a client_id-bearing Singpass request`, but `network.jsonl` had the redirect anyway, `id.singpass.gov.sg/fapi/auth?client_id=...`, with a `302` response the browser followed all the way to the real QR page. The passive `context.on("request", ...)` listener saw it; `context.route()` never called `_intercept` for it at all, so this browser fetched and consumed it for real despite `capture_only` being active. The cause was passing a compiled Python `re.Pattern` straight to `context.route()`, which Playwright's Python bindings re-marshal into its Node-based driver as a separate regex reconstruction, a different code path than the `.search()` calls already proven correct elsewhere in this script. `_is_singpass_auth_request()` replaced it with a plain Python predicate, evaluated locally, no cross-language regex reconstruction involved. Verified offline against a fake matching URL: a `fetch()` to it from inside a test page was aborted before any network I/O happened, "Failed to fetch" on the JS side, and the URL was still captured.

The very same `[FAIL] never intercepted` recurred on the next live run, `login_opened: same_tab` in that run's `findings.json`, `network.jsonl` again holding the un-aborted `fapi/auth` request with its real `302`. The predicate fix caught a `fetch()` offline but not this actual same-tab top-level navigation live, so request selection moved out of `context.route()`'s own matcher entirely. `context.route("**/*", _intercept)` now routes every request, and `_intercept` itself decides abort versus `route.continue_()` in plain Python. Verified offline against a real top-level `page.goto()` navigation this time, not a `fetch()`, aborted and captured exactly as intended, and a second offline check confirmed a non-matching navigation still loads normally through the same handler.

The next live run appeared to write nothing to its log at all, but `ps` showed the process and its Chromium children still alive minutes later, not crashed. Python fully buffers `stdout` when it is redirected to a file instead of a terminal, so every `print()`, including the clickable link, was sitting in the buffer rather than reaching the log while the run sat in a retry-timeout wait with nothing visible to act on. `sys.stdout.reconfigure(line_buffering=True)` at import time forces each `print()` to flush immediately. Confirmed offline: redirecting the refusal path to a file showed its output immediately rather than only on process exit.

With buffering fixed, the next live run's log streamed correctly and showed the real remaining state: branch 1 answered `[INFO] no redirect URL intercepted` again, a third distinct interception approach now failing the same way, fell back to branch 2, reached the real QR, and timed out waiting for approval. Its `network.jsonl` held two separate `fapi/auth` requests with two different `request_uri` values, meaning branch 1's own click-through had already completed a real, silent navigation to Singpass, interception failing without us noticing the browser had moved, before branch 2 threw that away and repeated the whole sequence from `MCF_HOME`, doubling the time before any QR was ever shown. `branch2-singpass-qr-latest.png`, refreshed every `QR_REFRESH_INTERVAL_MS` during the wait, now covers a human checking back later than the QR's own short lifetime, and `--approval-timeout-s`'s default moved from `180` to `900` to match realistic pacing in an interactive session rather than a fixed script run.

For branch 1 itself, `playwright.chromium.launch()`'s reported `executable_path` names regular Chromium, but the actual live process tree ran `chrome-headless-shell`, a separate, leaner binary Playwright defaults to for headless launches, confirmed by comparing that property against `ps` during an actual launch. `channel="chromium"` forces the full regular binary instead, confirmed by the same `ps` comparison showing `chrome-linux64/chrome` in place of `chrome-headless-shell`. Verified offline against a real link click, not just a `goto()` or a `fetch()`, still intercepted and captured correctly under the new channel.

`channel="chromium"` did not fix branch 1, `[INFO] no redirect URL intercepted` a fourth time, so the leaner headless-shell binary was not the cause either. That run then crashed outright, `findings.json` stuck at `result: "incomplete"` with no explanation, and the user reported reaching and completing a real Singpass login on branch 2's QR before it did, evidence the run never captured because of the crash. The traceback named the actual bug: `QR_REFRESH_INTERVAL_MS` was referenced in the refresh-loop's print and its wait-slice calculation but never defined, only `RETRY_INTERVAL_MS` existed, a plain `NameError` on the very first print of that block, not a page crash mid-wait as the earlier defensive `except Exception` around the loop had assumed. `QR_REFRESH_INTERVAL_MS = 15000` is now defined alongside `RETRY_INTERVAL_MS`. `.dev/dev-env` gained `pyflakes`, added to its `pyproject.toml` and synced per the **development env** skill's own convention, run against the script afterward with no findings, a static check this class of bug should have been caught by before ever reaching a live run that spends a real login attempt. The defensive `except Exception` and the top-level `except Exception` added around branch 2's wait stay in place regardless, so a genuine page crash next time still lands in `findings.json` instead of losing the run's evidence.

Four fix attempts, compiled regex, Python predicate, route-everything, then `channel="chromium"`, have not produced a confirmed root cause for branch 1, only ruled out those four specific explanations. Every offline reproduction proved the matching logic correct in isolation, which was the wrong thing to keep re-proving: none of them showed whether `_intercept` runs at all during the actual live MCF/Singpass flow, only that it runs correctly for a synthetic page. `route_calls` now counts every invocation of the handler, matched or not, and every `singpass.gov.sg` URL it sees, printed and written to `findings.json` right where branch 1 concludes. A live run's log will now show one of two very different diagnoses directly: `route_calls.total` at or near `0` means the handler is not engaging with this real page at all, a registration or context problem; a high total with `route_calls.singpass` still `0` at the moment the real redirect fires means the handler runs but this one URL specifically slips past it. Verified offline that the counters increment correctly against a real link-click interception.

The user asked for actual research rather than another guess. Playwright's own docs, not a forum post, name a documented limitation matching this exact symptom: `context.route()` does not intercept a request a Service Worker handles first, since the worker's own `fetch` handler runs ahead of Playwright's routing layer, while a passive `context.on("request")` listener still sees it, CDP's Network events fire regardless of a service worker's involvement. That is exactly the pattern across every failed run, present in `network.jsonl`, never reaching `_intercept`, and it explains why every offline reproduction passed: none of those synthetic test pages ever registered a service worker, so none of them could have reproduced this. `browser.new_context(service_workers="block")` is the documented fix. Verified offline on two fronts: interception still catches a real link-click navigation with service workers blocked, and an ordinary page with no service worker still loads normally under the same context.

`service_workers="block"` did not fix it either. Instrumentation added to count every route-handler invocation, matched or not, `.dev/logs/20260922-160925-17.PB-probe/`, showed the real pattern: 9 requests routed, 0 for `singpass.gov.sg`, meaning the handler runs but stops engaging entirely somewhere around the cross-origin navigation, not a matching problem within reach of another pattern-matching fix. Asked to research rather than guess again, found several open Playwright/Chromium reports of interception losing sync specifically across cross-origin renderer-process swaps, with no documented one-line fix. With four fix attempts against a confirmed engine-level limitation and branch 2 already proven working end to end, a live run reached and completed a real Singpass login on branch 2's QR, decision: drop the redirect-capture path from the primary flow. `--wait-for-approval` no longer attempts it at all, going straight to the real QR page. `--capture-only` stays as a standalone diagnostic in case the lower-level CDP `Network.setBlockedURLs` primitive is worth trying later. `_check_authenticated()`, `before_cookies`, and `RETRY_INTERVAL_MS`, all specific to the dropped retry-check, were deleted rather than left as dead code.

That simplification uncovered a real bug of its own: the crash the user's successful login ran into was `NameError: QR_REFRESH_INTERVAL_MS`, referenced in the refresh loop but never defined, not a page crash as the surrounding `except Exception` had assumed. `.dev/dev-env` gained `pyflakes`, synced per the **development env** skill's own convention, and now runs clean against the script, a static check this exact class of bug should never have reached a live run in the first place.

With the on-page QR now the sole path, decoding it directly became worth doing rather than relying on the human's phone camera alone: `env` gained `opencv-python-headless` for `cv2.QRCodeDetector`, matching `ARCH-RUN-05`'s dependency scope, since this is now operational browser-automation code, not throwaway probing. `_decode_qr_element()` reads the QR straight from the DOM element `_wait_for_qr_render()` already locates, an `<img>`'s own base64 `data:` payload needing no screenshot at all, a `<canvas>` getting an element-scoped Playwright screenshot instead of a full-page one. A first offline test against a `cv2`-generated QR failed decoding entirely, even directly, no DOM or screenshot involved; the actual cause was the freshly encoded QR being a bare 37×37 image with no quiet-zone border, which fails detection regardless of source. `_decode_qr_element()` now upscales with nearest-neighbor interpolation and pads a white border before decoding. Verified offline end to end on both paths, a real generated QR embedded as a data-URI `<img>` and rendered into a live `<canvas>`, decoded back to the exact original URL each time.

A live run reached the real QR and answered `[INFO] ... could not decode a QR from it` on every refresh. Manually cropping the QR's own region straight out of the saved full-page screenshot and decoding that crop worked perfectly, `swk_qr_ref=g0EE6Dg6f-Fj85EwhR8vX` came back exactly, which meant the decoder and the QR itself were never the problem. Enumerating every element `QR_ELEMENT_SELECTOR` actually matched against that run's saved HTML showed the bug precisely: two decorative icon `<img data:image/svg+xml,...>` elements, 162×162 and 24×24, neither of them the QR. The real QR turned out to be `<svg id="ndi-qr-canvas" data-testid="ndi-qr-canvas" viewBox="0 0 37 37" role="img">`, an inline vector element, never a `<canvas>` or a raster `data:` image at all, so the selector's two alternatives never had a chance to match it, only the unrelated icons that happened to satisfy `img[src^='data:image']`.

`QR_ELEMENT_LOCATORS` replaces the combined selector with `LOGIN_LOCATORS`' own explicit-priority pattern, `#ndi-qr-canvas` and `[data-testid="ndi-qr-canvas"]` first, `canvas`, `svg[role="img"]`, and the old `img[data:image]` kept only as a last-resort fallback, so DOM order can never again let an unrelated same-selector element win. `_decode_qr_element()`'s existing screenshot path already covered `<svg>` correctly, since Playwright's element screenshot renders whatever is on screen regardless of element type, so only the selection logic needed the fix. Verified against the exact HTML that failed live: `_first_match()` now picks `id=ndi-qr-canvas` and decodes it to the identical `swk_qr_ref` value the manual crop found, and both synthetic fallback paths, `<img data:...>` and `<canvas>`, re-verified unbroken by the refactor.

The next live run decoded the QR correctly twice, confirming the element-selection fix live, then the QR rotated and later refreshes failed to decode again, a separate, lower-priority timing question not yet chased. The user pointed out a design gap the decode success exposed: printing the decoded URL as a clickable link is not useful on a desktop machine, Singpass authenticates through its phone app, not a browser, so what the human actually needs is the QR rendered on screen to point a camera at, not a string to click. `_save_qr_popup()` writes the QR element's own cropped image to a fixed `qr-code.png`, overwritten in place on every refresh rather than accumulating one file per step, and `_open_image_popup()` launches it in the OS's own viewer, `explorer.exe` against a `wslpath`-converted path first since this runs under WSL2, `xdg-open` as the native-Linux fallback. `explorer.exe`'s own exit code is a documented WSL interop quirk, unreliable even on success, so launch success here means the command was attempted, not that a window was confirmed seen, a limitation this environment gives no way to check directly. Verified offline that the saved file is a valid non-empty PNG and that the launch call completes without raising; whether a window actually appears on screen needs confirming on the next live run. Popup relaunch is suppressed once one has opened, so a 15s refresh cycle updates the same file in place instead of spawning a new window each time.

Refocusing on actually closing 17.PB.02 rather than side quests: the callback-detection wait only confirms the URL changed back to `mycareersfuture.gov.sg`, the same signal MCF's own home page and Singpass's QR page both proved unreliable on its own, a client-rendered app still showing its loading skeleton at that instant. A `qr-post-approval` snapshot taken immediately risked capturing exactly that skeleton instead of any account-identifying content, so `_wait_for_render(qr_page)` runs before that snapshot now, the same fix already proven twice elsewhere in this script. The authenticated page's visible text is also now printed straight to the console once callback is confirmed, not just saved to a file the human has to go open separately, so a successful run means actually seeing the account-identifying fields scroll by, 17.PB.02's real target. Verified offline that the extraction call itself is sound against synthetic authenticated-looking content.

That run answered 17.PB.01 for real. `.dev/logs/20260922-172553-17.PB-probe/`: the QR decoded, the popup opened, the user scanned it with their own phone and approved a real Singpass login, `qr-singpass-qr-latest` caught the mid-navigation moment, `https://www.mycareersfuture.gov.sg/dashboard/career`, the render-wait wasn't skipped this time, and `qr-post-approval` landed on the fully rendered authenticated dashboard. Task 17.12 closes and the **Session coordinator and browser runner** design note is updated in place with that finding.

17.PB.02 was marked closed too fast. The dashboard's printed text held `Hello <INITIAL_USER_NAME>,` and nothing else identity-bearing, and a display name alone does not satisfy scope item 06's account-confirmation check, so learning that the dashboard shows only a name answers what the dashboard contains, not what the probe was actually sent to find. The same captured page held `<button data-testid="initial-circle-container">`, a user-initials menu trigger the run never clicked, inside a `data-testid="dropdown-menu"` wrapper, government portals typically put the fuller account detail behind a profile or account settings page, not the dashboard itself. `ACCOUNT_MENU_TRIGGER` and `PROFILE_LINK_LOCATORS` extend the post-approval flow to click that menu and follow any profile or account link it reveals, printing whatever that page shows too, so one more real login checks both places instead of costing a second attempt later. Verified offline on two fronts: the exact trigger selector matches once against the real captured dashboard HTML, and the profile-link matching correctly picks a "My Profile" link out of representative dropdown content, the real revealed menu markup itself not being available until this actually runs. Task 17.13 and this probe's own finding above are reopened until that run either produces a stronger identifier or confirms the display name really is all MCF exposes anywhere reachable from the dashboard.

The user confirmed by hand that the account-menu dropdown shows the logged-in user's email directly, no profile page needed. `_find_email()` scans the dashboard text, the opened menu's text, and any profile page reached, for the first email address found, flagged explicitly, `[PASS] email found under the account menu: ...`, rather than left for a human to spot inside a text dump.

That email is exactly scope item 06's account-confirmation check, so the probe now rehearses that check live rather than stopping at extraction. `EASYMCF_ACCOUNT_EMAIL` stands in for the signed-in easyMCF user's own account, since this probe has no real sign-in of its own, task 17.09's real implementation reads that from the actual session, not a probe constant. Found against found, the script prints whether the MCF account matches, then prompts for the user's own real interactive confirmation, `input()`, not an assumed answer, a live rehearsal of the exact UX scope item 06 describes: explicit confirmation before the connection becomes usable, blocked otherwise.

Fixed a real bug caught by `pyflakes` before it reached a live run: the edit that added this had silently swallowed the `elif stopped_responding:` line, so a successful run would have overwritten its own correct `"authenticated via the on-page QR"` result with a `"stopped responding"` failure at the very end. Restored and reverified the full `if callback_seen: / elif stopped_responding: / else:` structure by hand. Compiled clean, `pyflakes` clean, the account-comparison logic sanity-checked offline for both the match and mismatch cases.

That run closed 17.PB.02 for real. `.dev/logs/20260922-175441-17.PB-probe/`: the QR decoded, the user scanned and approved a real Singpass login, the dashboard rendered, the account-menu click revealed `<INITIAL_USER_EMAIL>` directly, the **Profile** link led to a full profile page repeating that email alongside the account's work history, skills, and resume filename, `[PASS] MCF account <INITIAL_USER_EMAIL> matches your easyMCF account <INITIAL_USER_EMAIL>` printed, and the run then paused on a real `input()` prompt, still alive several minutes later, confirmed against `ps`, not crashed, until the user answered it in their own terminal. Task 17.13 closes. Both probe tasks, 17.12 and 17.13, are now closed against real live evidence, and task 17.02, design, is unblocked.

Sources: [Service Workers | Playwright](https://playwright.dev/docs/service-workers), [BrowserContext | Playwright](https://playwright.dev/docs/api/class-browsercontext)

### 2. Data model and seed data stubs, 17.16

edit locations: `17.EL.02` to `17.EL.08`

Edited `easymcf/db/schema.sql` to version 8 exactly as `17.EL.02` lists it and set `SCHEMA_VERSION = 8`, added the `mcf_attempt` scope predicate to `easymcf/tenancy.py`, and applied the schema standalone through raw `sqlite3` first, `PRAGMA table_info` confirming both `mcf_session`'s two new columns and `mcf_attempt`'s full column set (log `010/17/20260922192700`). Updated `scripts/gen_test_data.py` per `17.EL.05`: both users' `mcf_session` rows seed `missing` with no `cookie_ref`, and user 1 retains its confirmed identity history. Two `mcf_attempt` rows give each terminal branch, historical `connected` for user 1 and `failed` with `error_code` set for user 2.

A first generation run with no `--anchor` argument regenerated all 17 seed files instead of the two this task touches, dating every row against today rather than the checked-in seed's own reference date, `17.IS.01` below. Regenerating with the same `--anchor 2026-09-21` the checked-in seed already uses wrote only `16_mcf_session.sql` and the new `17_mcf_attempt.sql` (log `20260922194035`), and `resetdb.py --seed` applied schema and seed together against the dev scratch database (log `20260922194041`).

Direct inspection with `scripts/db_util.py` read both seeded rows back exactly as generated, `meta.schema_version = 8` (log `20260922194302`), then deliberately probed the two new constraints by hand: an out-of-vocabulary `status` was rejected by the `CHECK`, and a second active-status row for a user already holding one was rejected by `ux_mcf_attempt_active`, while a first active row for the other user succeeded (log `010/17/20260922192742`). Those two probe inserts were scratch data outside the checked-in seed, so `resetdb.py --seed` ran again to restore the dev database to the exact seeded state before any test suite touched it (log `010/17/20260922192752`).

Added `test_mcf_attempt_status_is_constrained` and `test_mcf_attempt_active_is_unique_per_user` to `tests/backend/test_schema_accounts.py`, matching the file's existing `test_one_mcf_session_per_user` and `test_running_run_is_unique_per_user_and_type` patterns, turning the two hand-probed constraints above into a durable regression pair. Added `test_seeded_mcf_connection_covers_both_branches` to `tests/backend/test_seed_data.py`, reading both seeded rows' full column set directly, and extended `test_two_users_with_distinct_data` with an `mcf_attempt` row count. `pyflakes` ran clean against every edited Python file first. Both new test files pass, 44 of 44 (log `20260922193213`), and the full non-live suite passes complete against the correctly anchored seed, 362 of 362 (log `20260922194049`).

### 3. Singpass browser automation seam, 17.07

edit locations: `17.EL.11` to `17.EL.16`

Built ahead of the coordinator, since `mcf_connection.py` imports it: `singpass_browser.py`, `singpass_fixture.py`, `singpass_live.py`, and the four fixture pages under `tests/fixtures/singpass/`. `pyflakes` ran clean. A standalone smoke script driving `FixtureSingpassBrowser` through every interface method end to end — `open_qr_login()`, `read_qr()`, `wait_for_authenticated()`, `read_account_email()`, `export_storage_state()`, `close()` — passed on the first real run once the fixture's own auto-navigate timer was tuned (below), confirming the QR decoded to the seeded reference link and the account email matched the profile fixture (log `20260923100837` failed on a QR-render race, log `20260923101045` passed after the fix).

`qr.html`'s client-side redirect to the dashboard fixture initially fired at 300ms, fast enough that `open_qr_login()`'s own render wait let the page navigate past the QR before the smoke script ever read it — a fixture-timing artifact, not a code defect, fixed by widening the delay to 2000ms for the smoke test and later to 8000ms once the frontend's own 3 second poll needed a wider window to reliably observe `awaiting_approval` (task 17.08, below). `tests/backend/test_singpass_browser.py`'s five tests then passed complete, 5 of 5 in 28.70 seconds (log `20260923101137`), each launching and closing its own headless Chromium instance.

### 4. Session coordinator and attempt lifecycle, 17.06

edit locations: `17.EL.09` to `17.EL.10`

`easymcf/services/mcf_connection.py` and `tests/backend/test_mcf_attempt.py`, `pyflakes` clean. The oracle file's first run failed 5 of 9 (log `20260923101447`): two tests asserted a raw `mcf_attempt` row count of 1 for user 1 without accounting for task 17.16's own seeded history, and the rest expected `account_confirmation_required` but landed on `interaction_required` — user 1's seeded `mcf_session.confirmed_account_email` already holds `demo.user@example.test`, so any stub email other than that one is a real mismatch, not a test bug in the coordinator. Reassigning those five tests to user 2, whose seed leaves the column `NULL`, fixed all five with no production code change (log `20260923101636`), 9 of 9.

A live test against the real Flask app (raw `curl`, signed in as `seed_b`, `POST /api/v1/mcf_attempt/start` through to `/confirm`) then hit a real defect the oracle tests, stubbing the browser entirely, could never have caught: `greenlet.error: cannot switch to a different thread`, `17.IS.04` below, a 500 on the very first `confirm()` call against a real Playwright-backed attempt. Fixed by moving browser ownership and the confirm/reject wait entirely into the background thread's own scope, communicating through a per-attempt control object instead of handing the browser object to the Flask request thread. `tests/backend/test_mcf_attempt.py` re-ran clean against the redesign with no test changes needed, 9 of 9 (log `20260923103218`), and the same `curl` sequence against the real app then completed the full start → QR → confirm → `mcf_session` write → disconnect cycle with no error.

### 5. QR and link handoff frontend, 17.08

edit locations: `17.EL.17` to `17.EL.30`

The named endpoints, both CAT-02 registrations, the blueprint registration, and the full frontend stack (`api-client.service.js`, `mcf-connect.service.js`, `mcf-badge`, `mcf-connect-modal`, `frontend/app/mcf/`, `nav-bar.directive.js`, `app.routes.js`, `index.html`, `app.css`). A `curl` smoke sequence against a real spawned app caught `17.IS.03` immediately: `GET /api/v1/mcf_session/search` answered `unknown table or unsupported operation`, since nothing had ever registered `mcf_session` as a readable resource before this feature (log `20260923101724`). Registering it alongside `mcf_attempt` fixed the same sequence end to end, including the cross-tenant `404`s on another user's attempt id (log `20260923101743`).

The full non-live suite then surfaced `17.IS.03`'s second half: `tests/backend/test_tenancy.py` auto-discovers every table in `tenancy.SCOPES` and failed four cases demanding an `OWNER_SQL` entry for `mcf_attempt`/`mcf_session` (log `20260923101749`), fixed with one query per table matching the file's own existing pattern (log `20260923102144`); the same run's one unrelated failure, a Google sign-in e2e test, passed cleanly in isolation immediately after (log `20260923102200`), a pre-existing flake this feature did not introduce.

`env/bin/python scripts/envcheck.py && env/bin/python scripts/resetdb.py --seed && env/bin/python -m easymcf`, then `scripts/ui_tester.py --case tests/frontend/checks/mcf_attempt.json`, per the **deploy-and-validation-cycle** skill. The first run failed the QR-reach check outright (log `20260923102351`): the fixture's 2000ms auto-navigate raced against the frontend's own 3 second poll, so the UI could skip past `awaiting_approval` entirely between two polls, the same class of timing gap task 17.07 already hit once, this time on the consuming side rather than the producer. Widening the fixture delay to 8000ms fixed the QR-reach check (log `20260923102531`) but exposed a second, genuine bug in the check design itself: splitting QR-reach and confirm across two checks that share one `ui_tester.py --case` server left an unconfirmed attempt from the first check for the second to collide with, `start_attempt`'s own active-row reuse correctly returning that stale row instead of a fresh one (log `20260923102643`). Merging the two into one self-contained check fixed it: all 3 checks pass (log `20260923103740`).

### 6. account confirmation and connection status page, 17.09

edit locations: `17.EL.31`

No new production code beyond what 17.06 and 17.08 already built — the mismatch check lives in `_run_attempt`, the confirmation and mismatch UI lives in the modal already shipped. `tests/e2e/test_mcf_connection_flow.py` exercises both scenarios against the real stack: the happy path confirms `mcf_session` writes correctly and the modal/badge reflect `connected`, and a seeded prior `confirmed_account_email` different from the fixture's own found email confirms the attempt lands on `interaction_required` with `mcf_session` untouched. Both pass, 2 of 2 in 42.75 seconds (log `20260923103841`).

### 7. disconnect and reauthentication handling, 17.10

edit locations: none beyond `17.EL.09`

`cancel()` and `mark_reauthentication_required()` are both already part of 17.06's own coordinator; the frontend's Connect/Disconnect conditional already keys off `mcf_session.status !== 'valid'`, true for `reauthentication_required` the same as `missing`. Verified directly against the real running app: `DELETE /api/v1/mcf_attempt/<id>` on a `connected` attempt cleared `mcf_session` to `missing` with `cookie_ref` `NULL` and no error; calling `mark_reauthentication_required(db, 1)` against the seeded `connected` user and loading `/mcf` as that user through a real Playwright page showed `status: expired`, the Connect MCF action, no Disconnect button, and a `MCF: EXPIRED` badge, all through the existing conditional with no code change.

### 8. nav icon and pop-up-only redesign, 17.IS.05 to 17.IS.11

edit locations: `17.EL.32` to `17.EL.45`

Direct user review of the running app, after tasks 17.06 to 17.10 closed, raised seven issues against the built UI and one against the app's own default mode: one nav icon instead of a link-plus-badge pair, the icon as MCF's own burgundy magnifying-glass mark, a pop-up rather than a separate `/mcf` page, current state only rather than a full attempt history, the login action hidden once already connected, a stoplight status dot on the icon itself, and `MCF_MODE` defaulting to `live` for this developer's own runs. All seven are design/behavior requests rather than runtime defects; each has its own full issue section above (`17.IS.05` to `17.IS.11`) since the user asked for that format specifically, with **hypothesis**/**diagnostic** framing adapted to a design change rather than a traceback.

Deleted `frontend/app/shared/mcf-badge/` and `frontend/app/mcf/` entirely, and the `/mcf` route. Built `frontend/app/shared/mcf-nav-icon/`, extended `mcf-connect.service.js` (`openPopup()`/`startAttempt()` split, `isActive()`, `statusLight()`, a post-confirm `refresh()`), and extended `mcf-connect-modal.directive.js` with the brief current-state block. `pyflakes` ran clean on the two touched Python files (`tests/conftest.py`, `easymcf/automation/singpass_browser.py`).

Validating the redesigned pop-up against the real fixture-mode app, rather than a stubbed browser, surfaced the session's two most consequential findings. First, setting `.env`'s `MCF_MODE=live` for `17.IS.11` would have silently leaked into the non-live test suite through `load_dotenv()`, since `MCF_MODE` was never in `conftest.py`'s existing env-scrub list the way the Google/session keys already are — confirmed by a standalone reproduction showing `Config().mcf_mode == 'live'` after importing `tests.conftest` with `MCF_MODE` pre-set, fixed by adding it to that same scrub block, and reconfirmed clean afterward. Second, real headless-Chromium automation on this machine, under real concurrent load from other work already running (11 Chromium processes observed via `ps`), took 40 to 90 seconds end to end where earlier isolated smoke tests had taken 1 to 2 seconds — not a defect, confirmed by a direct `curl` sequence completing the full attempt lifecycle correctly every time, just slowly. `QR_RENDER_TIMEOUT_MS` widened from 20000ms to 45000ms in production code as a genuine robustness fix, `tests/e2e/test_mcf_connection_flow.py` gained an explicit `@pytest.mark.timeout(180)` and 60 second Playwright waits, passing twice in direct succession (~41s, then ~42s), and `tests/frontend/checks/mcf_attempt.json`'s own connect-flow check was simplified to stop at the QR step, already reliable at a 120 second budget (log `20260923114708`), leaving the full connect-through-confirm assertion to the pytest e2e tier rather than duplicating it under `ui_tester`'s heavier one-server-per-file resource profile. The full non-live suite passed complete afterward, 382 of 382 (log `20260923115556`).

## Validate

_the evidence for tasks 17.16 onward is recorded here as each task runs; probe evidence for 17.PB is the directory-shaped evidence already cited throughout **Implement** step 1 and the **Probe script design** section, not repeated as flat log rows below_

Log artifacts (`.dev/logs/`, gitignored - see commands below to inspect or reproduce; logs 01 to 06 route into `.dev/logs/010/17/`, this repo's log-router subdirectory for feature 17, and 07 onward sit at `.dev/logs/` directly):

| id | log file                                             | exit | evidence                                 |
| -- | ---------------------------------------------------- | ---- | ---------------------------------------- |
| 01 | `20260922192700-17.16-schema-sqlite-check.log`       | 0    | schema applies, columns confirmed        |
| 02 | `20260922192704-17.16-seed-generate.log`             | 0    | writes 17 files, unanchored (`17.IS.01`) |
| 03 | `20260922192719-17.16-resetdb-seed.log`              | 0    | schema and seed load on version 8        |
| 04 | `20260922192732-17.16-db-util-inspection.log`        | 0    | seeded rows read back as generated       |
| 05 | `20260922192742-17.16-db-util-constraints.log`       | -    | CHECK and active-index constraints hold  |
| 06 | `20260922192752-17.16-resetdb-seed-restore.log`      | 0    | dev db restored to unanchored seed       |
| 07 | `20260922193213-17.16-schema-seed-tests.log`         | 0    | 44 of 44, schema and seed oracles        |
| 08 | `20260922193224-17.16-full-suite-final.log`          | 0    | 362 of 362, surfaced `17.IS.01`          |
| 09 | `20260922194035-17.16-seed-generate-anchored.log`    | 0    | fix: only 2 files changed                |
| 10 | `20260922194041-17.16-resetdb-seed-anchored.log`     | 0    | schema and anchored seed, version 8      |
| 11 | `20260922194049-17.16-full-suite-anchored.log`       | 0    | 362 of 362, against corrected seed       |
| 12 | `20260922194302-17.16-db-util-inspection-anchor.log` | 0    | seeded rows dated 2026-09-21             |
| 13 | `20260923100837-17.07-fixture-smoke.log`             | 1    | QR-render race, fixed in log 14          |
| 14 | `20260923101045-17.07-fixture-smoke.log`             | 0    | full fixture flow passes standalone      |
| 15 | `20260923101137-17.07-test-singpass-browser.log`     | 0    | 5 of 5, automation-silo tier             |
| 16 | `20260923101447-17.06-test-mcf-attempt.log`          | 1    | 4 of 9, seed-state test bug              |
| 17 | `20260923101636-17.06-test-mcf-attempt-fixed.log`    | 0    | 9 of 9, against stubbed browser          |
| 18 | `20260923101724-17.08-api-smoke.log`                 | 0    | start/poll/qr/confirm, 17.IS.03 seen     |
| 19 | `20260923101743-17.08-api-smoke-2.log`               | 0    | `mcf_session` read fixed                 |
| 20 | `20260923101749-17.08-full-suite-check.log`          | 1    | 5 failed: tenancy, one flake             |
| 21 | `20260923102144-17.08-tenancy-fix.log`               | 0    | 44 of 44, ownership rows added           |
| 22 | `20260923102200-17.08-google-e2e-recheck.log`        | 0    | unrelated flake passes alone             |
| 23 | `20260923102351-17.08-ui-tester-mcf.log`             | 1    | QR step timed out, timing race           |
| 24 | `20260923102531-17.08-ui-tester-mcf-2.log`           | 1    | QR fixed, next check now stale           |
| 25 | `20260923102643-17.08-ui-tester-mcf-3.log`           | 1    | confirmed cause, checks split            |
| 26 | `20260923103218-17.06-test-mcf-attempt-redesign.log` | 0    | 9 of 9, threading redesign               |
| 27 | `20260923103740-17.08-ui-tester-mcf-4.log`           | 0    | 3 of 3, checks merged                    |
| 28 | `20260923103841-17.09-e2e-mcf-connection-flow.log`   | 0    | 2 of 2, happy path, mismatch             |
| 29 | `20260923104922-17.11-full-suite-final.log`          | 0    | 382 of 382, closing run                  |
| 30 | `20260923112449-17.IS05-11-ui-tester.log`            | 1    | QR-image timeout, 15s budget too tight   |
| 31 | `20260923113857-17.IS05-11-ui-tester-3.log`          | 1    | QR-image still short at 60s              |
| 32 | `20260923114031-17.IS05-11-ui-tester-4.log`          | 1    | QR fixed at 120s, confirm-yes now short  |
| 33 | `20260923114708-17.IS05-11-ui-tester-6.log`          | 0    | 3 of 3, check simplified to QR-reach     |
| 34 | `20260923115556-17.IS05-11-full-suite-final.log`     | 0    | 382 of 382, closing run                  |
| 35 | focused Feature 17 suite                            | 0    | 41 of 41 after mode-source repair        |
| 36 | final envcheck and full fixture suite               | 0    | envcheck and 408 of 408                  |

**secondary validation** - reproduce independently:

```bash
env/bin/python -c "
import sqlite3
conn = sqlite3.connect(':memory:')
conn.executescript(open('easymcf/db/schema.sql').read())
print([r[1] for r in conn.execute('PRAGMA table_info(mcf_attempt)')])
"
env/bin/python scripts/db_util.py mcf_attempt search '{}'
env/bin/python -m pytest -q tests/backend/test_schema_accounts.py tests/backend/test_seed_data.py

# the full feature, end to end, against the real app and fixture-mode Singpass:
env/bin/python scripts/envcheck.py
env/bin/python scripts/resetdb.py --seed
env/bin/python -m easymcf &
scripts/ui_tester.py --case tests/frontend/checks/mcf_attempt.json
env/bin/python -m pytest -q tests/backend/test_mcf_attempt.py tests/backend/test_singpass_browser.py tests/e2e/test_mcf_connection_flow.py
kill %1
```

**Issues**

- inventory all first out exceptions and issues encountered in this table
- for each issue, create an issue section and use it to document diagnostics and resolution steps - see
  `validation/REFERENCE.md` and its `templates/issue-diagnostic-section.md` boilerplate

| id       | seq | status  | issue                                                               |
| -------- | --- | ------- | ------------------------------------------------------------------- |
| 17.IS.01 | 01  | closed  | seed regeneration with no `--anchor` shifts every seed file's dates |
| 17.IS.02 | 02  | closed  | `mcf_attempt.status='expired'` names no reachable outcome           |
| 17.IS.03 | 03  | closed  | `mcf_session` has no read endpoint, and no tenancy oracle entry     |
| 17.IS.04 | 04  | closed  | `confirm()`/`cancel()` touch a browser object from another thread   |
| 17.IS.05 | 05  | closed  | the nav bar carries two separate MCF entries, a link and a badge    |
| 17.IS.06 | 06  | closed  | the nav icon is plain text, not MCF's own mark                      |
| 17.IS.07 | 07  | closed  | the MCF status lives on a separate nav-destination page             |
| 17.IS.08 | 08  | closed  | the status page renders a full attempt history                      |
| 17.IS.09 | 09  | closed  | Connect MCF stays available on an already-valid session             |
| 17.IS.10 | 10  | closed  | the icon carries no session-status signal of its own                |
| 17.IS.11 | 11  | closed  | the app defaults to fixture mode for a human's own local run        |
| 17.IS.12 | 12  | closed  | MCF nav icon uses green instead of the maroon icon background       |
| 17.IS.13 | 13  | closed  | MCF indicator appears inside the icon instead of beside it          |
| 17.IS.14 | 14  | closed  | MCF status pop-up disappears before its connected state is usable   |
| 17.IS.15 | 15  | closed  | Singpass QR expires before the user receives it                     |
| 17.IS.16 | 16  | closed  | mcf pop-up color shaded bg not foreground                           |
| 17.IS.17 | 17  | closed  | MCF confirmation appears after failed Singpass authentication       |
| 17.IS.18 | 18  | closed  | mcf confirmation pop-up shaded background                           |
| 17.IS.19 | 19  | closed  | active authenticated browser closes without clicking MCF Logout     |
| 17.IS.20 | 20  | closed  | Login popup bypasses the browser page used for QR capture           |
| 17.IS.21 | 21  | closed  | confirmed MCF user is prompted for confirmation again               |
| 17.IS.22 | 22  | closed  | mcf status pop-up open authenticated session in new browser         |
| 17.IS.23 | 23  | closed  | closing envcheck finds the earlier live server on port 5000          |
| 17.IS.24 | 24  | closed  | headed browser restores saved state but MCF still shows Login        |
| 17.IS.25 | 25  | closed  | UI remains connected after restored MCF session proves invalid       |
| 17.IS.26 | 26  | closed  | fresh authentication cannot read the MCF account email               |
| 17.IS.27 | 27  | closed  | user closing headed browser logs `TargetClosedError` as failure      |
| 17.IS.28 | 28  | closed  | headed launcher imports unavailable public `TargetClosedError`       |
| 17.IS.29 | 29  | closed  | seed claims authentication without seeding browser state             |
| 17.IS.30 | 30  | closed  | status poll reports valid when authenticated Open must fall back     |
| 17.IS.31 | 31  | closed  | restart script launches the fixture Singpass flow                    |

_17.IS.01 (closed) seed regeneration with no `--anchor` shifts every seed file's dates_

**problem description**

Running `scripts/gen_test_data.py --mode seed --out seed/ --rand-seed 42` for task 17.16, to add the `mcf_attempt` rows and `mcf_session` columns, regenerated all 17 seed files rather than only the two this task's own data touches: `git status --short seed/` showed 8 files beyond `16_mcf_session.sql` and the new `17_mcf_attempt.sql` modified, `02_user.sql`, `07_run_log.sql`, `08_post.sql`, `11_lead.sql`, `12_lead_note.sql`, `13_lead_event.sql`, `14_offer.sql`, and `15_application.sql`.

**exception**

```log
<no error - the generator printed [PASS] seed generated and exited 0, and the full non-live suite passed 362 of 362 against the regenerated seed; the defect was a silent, unrequested change to unrelated checked-in files, not a failure any log line reported>
```

**triggering actions**

01. run `env/bin/python scripts/gen_test_data.py --mode seed --out seed/ --rand-seed 42` with no `--anchor` argument (log `20260922192704`).
02. run `env/bin/python scripts/resetdb.py --seed`, direct `db_util.py` inspection, and the full non-live suite; all pass (logs `20260922192719` through `20260922193224`).
03. run `git status --short seed/` while reviewing the change set for this task, and see 8 files beyond the two this task should touch.

**hypothesis**

`build_sql()`'s `anchor` parameter defaults to `date.today()` in `main()` whenever `--anchor` is omitted, and every date-bearing `INSERT` across every table is computed relative to that one anchor, not only the two tables this task adds columns and rows to. The checked-in seed carries `2026-09-21` throughout, one day behind this session's system date of `2026-09-22`, so any regeneration that omits `--anchor` reproduces the full seed dataset shifted forward by exactly that gap.

**diagnostic steps**

| id          | seq | status | step                                                                |
| ----------- | --- | ------ | ------------------------------------------------------------------- |
| 17.IS.01.01 | 01  | closed | diff the unexpectedly modified files against their checked-in form |
| 17.IS.01.02 | 02  | closed | confirm the checked-in seed's own reference date from git history  |
| 17.IS.01.03 | 03  | closed | regenerate with `--anchor` pinned to that same date and re-diff    |

**diagnostic details**

01. (closed) `git diff seed/02_user.sql` and `git diff seed/07_run_log.sql` showed only `2026-09-21` timestamps changing to `2026-09-22` in every `created_at`/`started_at`/`ended_at` field, no other content difference in either file. This matches the hypothesis: a pure date shift, not a logic change.
02. (closed) `git log -p -1 -- seed/02_user.sql` showed the commit that last wrote it carrying `created_at = '2026-09-21 06:00:00'`, confirming the checked-in seed's own anchor is `2026-09-21`, the day before this session's date rather than a coincidence.
03. (closed) `env/bin/python scripts/gen_test_data.py --mode seed --out seed/ --rand-seed 42 --anchor 2026-09-21` (log `20260922194035`), then `git status --short seed/` showed only `16_mcf_session.sql` modified and `17_mcf_attempt.sql` new, the two files this task actually changes. `resetdb.py --seed` (log `20260922194041`), direct `db_util.py` inspection confirming both seeded rows dated `2026-09-21` (log `20260922194302`), and the full non-live suite (log `20260922194049`) all passed against the corrected seed, 362 of 362.

_17.IS.02 (closed) `mcf_attempt.status='expired'` names no reachable outcome_

**problem description**

The schema's own `CHECK` constraint and the Design section's own state list both name ten `mcf_attempt` statuses including `expired`, distinct from `reauthentication_required`, but writing `tests/backend/test_mcf_attempt.py`'s state-machine oracles surfaced that nothing in the coordinator ever set an attempt row to `expired`: the Design section's own text names it as one of the states `ux_mcf_attempt_active` excludes, but never says what event produces it.

**exception**

```log
<no error - the gap surfaced while enumerating every state the state-machine oracle (17.TC.13) needed to reach, not from a failing assertion or a traceback>
```

**triggering actions**

01. read the **Data model** and **Expiry and reauthentication** design sections while drafting `test_mcf_attempt.py`'s test list, checking every one of the ten `CHECK`-listed values has a corresponding test.
02. find `reauthentication_required` fully specified (an already-`connected` session lapsing later, `mark_reauthentication_required`) but no equivalent sentence anywhere naming what produces `expired`.

**hypothesis**

`wait_for_authenticated()`'s own bounded wait (`AUTHENTICATED_WAIT_MS`, 15 minutes) is the one place in the attempt flow with a real timeout separate from a hard error: a QR the user never scans in time. That distinct, ordinary outcome, not a `failed` hard error, is the natural fit for the vocabulary's unused `expired` value.

**diagnostic steps**

| id          | seq | status | step                                                                  |
| ----------- | --- | ------ | --------------------------------------------------------------------- |
| 17.IS.02.01 | 01  | closed | catch the approval wait's own timeout and map it to `expired`      |
| 17.IS.02.02 | 02  | closed | add a stubbed-timeout oracle asserting `expired`, not `failed`     |

**diagnostic details**

01. (closed) Added a `try`/`except PlaywrightTimeoutError` around `browser.wait_for_authenticated()` specifically, distinct from the generic `except Exception` around the rest of `_run_attempt`, setting `status='expired'` with no `error_code` on that one path.
02. (closed) `test_approval_timeout_lands_on_expired_not_failed`, a `FakeBrowser(timeout=True)` whose `wait_for_authenticated()` raises `PlaywrightTimeoutError`, confirmed the row lands on `expired` with `error_code IS NULL`, distinct from `test_unexpected_error_lands_on_failed_with_its_type_as_error_code`'s `RuntimeError` case landing on `failed` with its type recorded. Both pass as part of the file's 9 of 9 (log `20260923101636`), and every value in the ten-state `CHECK` vocabulary now has a reachable, tested path.

_17.IS.03 (closed) `mcf_session` has no read endpoint, and no tenancy oracle entry_

**problem description**

A `curl` smoke sequence against the real running app, signed in and polling after `POST /api/v1/mcf_attempt/start`, answered `{"error": "not_found", "message": "unknown table or unsupported operation: mcf_session"}` on `GET /api/v1/mcf_session/search` — a table this feature's own Design section assumes the frontend already polls.

**exception**

```log
{"error":"not_found","message":"unknown table or unsupported operation: mcf_session"}
```

**triggering actions**

01. sign in as `seed_b` against a real `python -m easymcf` process and `POST /api/v1/mcf_attempt/start` (log `20260923101724`).
02. `GET /api/v1/mcf_session/search` against the same signed-in client.

**hypothesis**

`mcf_session` was added to the schema in feature 13 for the manual-upload flow that feature's own scope replaced before it was ever built, and its only writers since have been `auth/accounts.py`/`auth/google.py` at signup, both internal calls with no client-facing read ever registered in `easymcf/api/resources.py`. `mcf_attempt` is a brand new table this feature adds, so `easymcf/tenancy.py`'s `SCOPES` dict and any table-driven test matrix would name only what this feature explicitly registers, potentially missing `mcf_session` too since it predates this feature's own edits.

**diagnostic steps**

| id          | seq | status | step                                                                |
| ----------- | --- | ------ | ------------------------------------------------------------------- |
| 17.IS.03.01 | 01  | closed | grep `easymcf/api/resources.py` for an existing `mcf_session` registration |
| 17.IS.03.02 | 02  | closed | register it read-only and re-run the smoke sequence                |
| 17.IS.03.03 | 03  | closed | run the full suite and address whatever else the registration surfaces |

**diagnostic details**

01. (closed) `grep -n "mcf_session" easymcf/api/resources.py` returned nothing: the table genuinely had no `Resource` registration at all, confirming the hypothesis directly rather than narrowing it.
02. (closed) Added `register(Resource("mcf_session", create_schema=_ANY, update_schema=_ANY, verbs=READ_ONLY))` next to the new `mcf_attempt` registration. The same `curl` sequence then returned the seeded row correctly, `status`, `cookie_ref`, and both confirmation columns all present (log `20260923101743`).
03. (closed) The full non-live suite then failed 4 cases in `tests/backend/test_tenancy.py` (log `20260923101749`): that file auto-discovers every table in `tenancy.SCOPES` and requires a matching `OWNER_SQL` oracle entry, so registering `mcf_attempt` and `mcf_session` both surfaced the gap at once. Added one `SELECT id FROM <table> WHERE user_id = 1 LIMIT 1` entry per table, matching every other scoped table's own pattern; the full 44-case tenancy matrix passed (log `20260923102144`), and the same run's one remaining failure, an unrelated Google sign-in e2e test, passed cleanly in isolation (log `20260923102200`), a pre-existing flake this feature did not introduce.

_17.IS.04 (closed) `confirm()`/`cancel()` touch a browser object from another thread_

**problem description**

A `curl` `POST /api/v1/mcf_attempt/<id>/confirm` against a real attempt that had genuinely reached `account_confirmation_required` through the real fixture-mode Singpass flow answered `500 Internal Server Error`.

**exception**

```log
File "easymcf/automation/singpass_browser.py", line 221, in export_storage_state
    self._context.storage_state(path=path)
  File "playwright/_impl/_sync_base.py", line 113, in _sync
    self._dispatcher_fiber.switch()
greenlet.error: cannot switch to a different thread (which happens to have exited)
```

**triggering actions**

01. sign in as `seed_b`, `POST /api/v1/mcf_attempt/start`, poll until `account_confirmation_required` against a real fixture-mode attempt (not a stub).
02. `POST /api/v1/mcf_attempt/<id>/confirm` with `{"accept": true}`.

**hypothesis**

`mcf_connection.py`'s original design stored the live `SingpassBrowser` object in the module's in-memory registry once the background thread reached `account_confirmation_required`, and had `confirm()`/`cancel()`, running on the Flask request thread, pop it out and call `export_storage_state()`/`close()` on it directly. Playwright's sync API is believed to be thread-affine internally (a greenlet-based dispatcher bound to the thread that started it), so a Playwright object created inside the background thread would not answer a call from the Flask request thread, regardless of locking.

**diagnostic steps**

| id          | seq | status | step                                                                  |
| ----------- | --- | ------ | --------------------------------------------------------------------- |
| 17.IS.04.01 | 01  | closed | confirm the thread-affinity constraint against the traceback       |
| 17.IS.04.02 | 02  | closed | redesign so the owning thread never hands its browser to another   |
| 17.IS.04.03 | 03  | closed | re-run the oracle suite, then the real `curl` sequence              |

**diagnostic details**

01. (closed) The traceback's own library frames (`playwright/_impl/_sync_base.py`'s `_sync`/`_dispatcher_fiber.switch()`) confirm the cause directly: every Playwright sync-API call dispatches through a greenlet fiber tied to the thread that first called `sync_playwright().start()`, and switching to it from a different OS thread raises exactly this error. `_run_attempt` runs on a `threading.Thread` it starts itself; `confirm()`/`cancel()` run on whichever thread Flask's WSGI server used for that request, a different thread by construction.
02. (closed) Redesigned `mcf_connection.py` so `_run_attempt`'s own thread owns its `SingpassBrowser` instance for the attempt's entire life, including the wait for a confirm/reject decision, via a per-attempt `threading.Event` and a decision flag; `confirm()` and `cancel()` now only ever set that flag and the event, never touch `browser`, per `17.EL.09`'s full listing above.
03. (closed) `tests/backend/test_mcf_attempt.py` re-ran clean against the redesign with no test changes needed, since the stubbed `FakeBrowser` was never thread-sensitive to begin with, 9 of 9 (log `20260923103218`). The same `curl` sequence against the real fixture-mode app then completed `start` → QR → `confirm` → `mcf_session` write with no error, and a follow-up `DELETE` disconnected cleanly, both confirmed directly against server output with no traceback.

_17.IS.05 (closed) the nav bar carries two separate MCF entries, a link and a badge_

**problem description**

`nav-bar.directive.js` rendered both a plain `<a href="/mcf">MCF</a>` text link and a separate `<mcf-badge>` status pill next to it. Direct user review of the running nav bar found this reads as two unrelated MCF controls rather than one.

**exception**

```log
<no error - a direct design review of the rendered nav bar, not a runtime failure>
```

**triggering actions**

01. load the app and look at the rendered nav bar next to Tracks/Leads/Offers.

**hypothesis**

One control covering both jobs, navigation and status, reads as a single MCF presence in the nav bar; two separate elements for the same feature reads as a duplicate.

**diagnostic steps**

| id          | seq | status | step                                                          |
| ----------- | --- | ------ | ------------------------------------------------------------- |
| 17.IS.05.01 | 01  | closed | remove the text link and the separate badge directive            |
| 17.IS.05.02 | 02  | closed | replace both with one icon element carrying both jobs (17.IS.06/.10) |

**diagnostic details**

01. (closed) Deleted `frontend/app/shared/mcf-badge/` and the `<a href="/mcf">` line from `nav-bar.directive.js`.
02. (closed) Added the single `<mcf-nav-icon>` directive in its place, covering navigation (opens the pop-up) and status (the stoplight dot) in one element — `17.EL.32` to `17.EL.37` below. Confirmed by direct inspection of the rendered nav bar: one MCF element, not two (screenshot `20260923112511`, and the passing `ui_tester` checks in `17.IS.07` below).

_17.IS.06 (closed) the nav icon is plain text, not MCF's own mark_

**problem description**

Before this pass the one nav entry (still two elements at the time) was the literal word "MCF" in the nav bar's own font, not any kind of icon or mark distinct from the other text links.

**exception**

```log
<no error - a design request for the icon's own visual identity, not a runtime failure>
```

**triggering actions**

01. direct request: use MCF's own burgundy magnifying-glass mark for the icon, not plain text.

**hypothesis**

An inline SVG magnifying glass in MCF's own burgundy is buildable with no new asset or icon font, consistent with `ARCH-RUN-06`'s no-build-step, no-external-asset constraint.

**diagnostic steps**

| id          | seq | status | step                                                         |
| ----------- | --- | ------ | ------------------------------------------------------------ |
| 17.IS.06.01 | 01  | closed | build the mark as an inline SVG circle-plus-handle, no icon font |
| 17.IS.06.02 | 02  | closed | render it in a burgundy stroke color, confirm by direct inspection |

**diagnostic details**

01. (closed) `mcf-nav-icon.directive.js`'s template is a small inline `<svg>`, a stroked circle and a handle line, the standard magnifying-glass shape, needing no icon font, sprite sheet, or build step.
02. (closed) Stroke color `#7a1f3d`, a burgundy approximating MCF's own site color, applied directly as an SVG attribute. Confirmed by direct inspection of the rendered nav bar (screenshot `20260923112511`) — the mark renders as a small burgundy magnifying glass, distinct from the plain-text nav links beside it. The exact hex is an approximation, not read from a captured MCF asset; a closer match is a one-line change to `mcf-nav-icon.directive.js` if the real brand value is ever supplied.

_17.IS.07 (closed) the MCF status lives on a separate nav-destination page_

**problem description**

Task 17.08 built a dedicated `/mcf` route and page (`frontend/app/mcf/`) as the connection's own home. Direct request: a pop-up, not a separate page, and brief.

**exception**

```log
<no error - a navigation-model design request, not a runtime failure>
```

**triggering actions**

01. direct request: the MCF page should be a pop-up, not a separate page, kept brief.

**hypothesis**

The existing `mcf-connect-modal` already covers the QR/confirm/mismatch steps; the same pop-up, extended with a brief current-state view for when nothing is in flight, removes the need for a dedicated page entirely, one component instead of two.

**diagnostic steps**

| id          | seq | status | step                                                             |
| ----------- | --- | ------ | ---------------------------------------------------------------- |
| 17.IS.07.01 | 01  | closed | delete the `/mcf` route and `frontend/app/mcf/`                       |
| 17.IS.07.02 | 02  | closed | add a brief current-state block to the existing pop-up (17.IS.08/.09) |
| 17.IS.07.03 | 03  | closed | validate the pop-up-only flow end to end against the real app         |

**diagnostic details**

01. (closed) Removed the `.when('/mcf', ...)` route from `app.routes.js`, deleted `frontend/app/mcf/`, and dropped its script tag from `index.html`. `mcf-connect-modal` is now the one place MCF connection state renders anywhere in the app.
02. (closed) See `17.IS.08`/`17.IS.09` for the current-state block's own content and behavior.
03. (closed) Validating the redesigned pop-up against the real fixture-mode app surfaced a genuine timing gap, not a defect in the redesign itself: the fixture QR page's own 8 second auto-navigate (task 17.08's own tuning) plus the real account-menu/profile navigation the coordinator performs routinely took well past the pop-up's original 15 second `ui_tester` budget under this machine's own current load — 11 concurrent Chromium processes at the time, from other work already running. `.dev/logs/20260923112449-17.IS05-11-ui-tester.log` and its immediate re-run `...113303...` both failed waiting on `mcf-qr-image` at 15000ms; widening to 60000ms (`...113857...`) still failed at the same step; widening `QR_RENDER_TIMEOUT_MS` itself from 20000ms to 45000ms in `easymcf/automation/singpass_browser.py` (a real production robustness fix, not just a test tuning, since the same latency applies to a genuine live run under load) and the check's own QR-wait to 120000ms let the QR step pass, but exposed the same class of gap one step later, `...114031...` and `...114315...` both failing waiting on `mcf-confirm-yes` at 60000ms and then 120000ms. The connect-through-confirm flow's own correctness was never in question — a direct `curl` sequence against the same server completed the full `start` → `awaiting_approval` → `verifying` → `account_confirmation_required` cycle correctly every time, just slowly (around 40 to 90 seconds wall clock under this load) — and `tests/e2e/test_mcf_connection_flow.py`'s own pytest-tier version of the same flow, given a genuinely generous `@pytest.mark.timeout(180)`, passed twice in direct succession. The `ui_tester` check was simplified to stop at the QR step, already reliable at a 120 second budget and sufficient to demonstrate the pop-up itself opens, connects, and reaches the QR (`...114708...`, 3 of 3 pass), leaving the full connect-through-confirm assertion to the pytest e2e tier, which does not share `ui_tester`'s one-server-per-file resource profile.

_17.IS.08 (closed) the status page renders a full attempt history_

**problem description**

`mcf.html` rendered every past `mcf_attempt` row in a table below the current status. Direct request: drop the history, current session state only.

**exception**

```log
<no error - a content-scope design request, not a runtime failure>
```

**triggering actions**

01. direct request: the MCF view should show current session state only, no history.

**hypothesis**

Deleting `frontend/app/mcf/` entirely (17.IS.07) already removes the only place the history table rendered; no separate removal is needed once the page itself is gone.

**diagnostic steps**

| id          | seq | status | step                                                                 |
| ----------- | --- | ------ | -------------------------------------------------------------------- |
| 17.IS.08.01 | 01  | closed | confirm the pop-up's own template never renders a per-row history     |
| 17.IS.08.02 | 02  | closed | add a direct check asserting zero history rows render anywhere        |

**diagnostic details**

01. (closed) `mcf-connect-modal.directive.js`'s template has no `ng-repeat` over attempts at all; its `mcf-current-state` block reads only `connect.state.session` and, for wording, the single latest `connect.state.attempt`, never a list.
02. (closed) `tests/frontend/checks/mcf_attempt.json`'s second check asserts `count_equals` on `[data-testid^="mcf-attempt-row-"]` is `0`; `tests/e2e/test_mcf_connection_flow.py`'s own happy-path test asserts the same. Both pass (logs `20260923114708` and the e2e run below).

_17.IS.09 (closed) Connect MCF stays available on an already-valid session_

**problem description**

The removed `mcf.html` showed the Connect MCF button whenever `mcf_session.status !== 'valid'`, correctly hiding it once valid — but `McfConnect.open()` (now `startAttempt()`) had no equivalent guard of its own, and the modal had no notion of "nothing in flight, already connected" separate from "nothing in flight, not connected" until this pass built one.

**exception**

```log
<no error - confirmed by direct reasoning about the rebuilt pop-up's own conditionals, not a runtime failure>
```

**triggering actions**

01. direct request: when a session is already active, disable the login action entirely.

**hypothesis**

The pop-up's own current-state block can key the same way the deleted page did — `session.status === 'valid'` shows Disconnect only, anything else shows Connect MCF only — with the addition that an attempt already in flight shows the QR/confirm steps instead of either, so there is never a moment offering a second, redundant Connect action.

**diagnostic steps**

| id          | seq | status | step                                                              |
| ----------- | --- | ------ | ----------------------------------------------------------------- |
| 17.IS.09.01 | 01  | closed | key the current-state block's buttons off `session.status` alone   |
| 17.IS.09.02 | 02  | closed | confirm by direct check: valid shows no Connect button at all      |

**diagnostic details**

01. (closed) `mcf-connect-modal.directive.js`'s current-state block: `ng-if="connect.state.session.status === 'valid'"` for Disconnect, `ng-if="connect.state.session.status !== 'valid'"` for Connect MCF — mutually exclusive, and the block itself only renders when `!connect.isActive(connect.state.attempt)`, so an in-flight attempt shows its own QR/confirm step instead of either button.
02. (closed) `tests/frontend/checks/mcf_attempt.json`'s first check asserts `visible` `false` on `mcf-connect` for the already-valid seeded user, alongside `visible` `true` on `mcf-disconnect`; passes (log `20260923114708`).

_17.IS.10 (closed) the icon carries no session-status signal of its own_

**problem description**

Before this pass, session status was only visible by opening the (now-removed) separate page or the old text badge; the nav icon itself carried no status of its own. Direct request: a stoplight indicator on the icon, opening the pop-up for detail.

**exception**

```log
<no error - a status-visibility design request, not a runtime failure>
```

**triggering actions**

01. direct request: the nav icon should show session status as a stoplight, with the pop-up for detail on click.

**hypothesis**

A small colored dot overlaid on the magnifying-glass icon, three colors mapped from the same states the pop-up already tracks (an attempt in flight, a valid session, anything else), gives an at-a-glance signal with no separate status text needed in the nav bar itself.

**diagnostic steps**

| id          | seq | status | step                                                                |
| ----------- | --- | ------ | ------------------------------------------------------------------- |
| 17.IS.10.01 | 01  | closed | add `McfConnect.statusLight()`, mapping state to green/amber/red       |
| 17.IS.10.02 | 02  | closed | overlay a colored dot on the icon, confirm each color by direct check  |

**diagnostic details**

01. (closed) `statusLight()` returns `'amber'` while `isActive(state.attempt)` is true (a connection attempt in flight), `'green'` when `state.session.status === 'valid'`, and `'red'` otherwise (missing or expired).
02. (closed) `.mcf-status-dot`/`.mcf-status-{green,amber,red}` in `app.css`, absolutely positioned over the icon's own corner. `tests/frontend/checks/mcf_attempt.json` asserts the exact dot class for the connected (`green`), never-connected (`red`), and in-flight (`amber`) cases; all three pass (log `20260923114708`).

_17.IS.11 (closed) the app defaults to fixture mode for a human's own local run_

**problem description**

`ARCH-RUN-08` deliberately defaults `MCF_MODE` to `fixture`, requiring an explicit opt-in to reach the real MCF/Singpass hosts. Direct request: default to a live connection so this feature can be validated against the real site.

**exception**

```log
<no error - a configuration-default change on explicit request, not a runtime failure>
```

**triggering actions**

01. direct request: default to live, ready for a human to validate against the real Singpass flow.

**hypothesis**

`.env` is exactly the mechanism `.env.example`'s own comment anticipates for this ("never set live except on explicit, human-confirmed ask"): setting `MCF_MODE=live` in the developer's own gitignored `.env`, rather than in `easymcf/config.py`'s hardcoded fallback, defaults this one human's own `python -m easymcf` runs to live while leaving the code-level default, and therefore every other consumer of `Config()`, untouched.

**diagnostic steps**

| id          | seq | status | step                                                                 |
| ----------- | --- | ------ | -------------------------------------------------------------------- |
| 17.IS.11.01 | 01  | closed | set `MCF_MODE=live` in `.env`, confirm `Config()` in code stays `fixture` |
| 17.IS.11.02 | 02  | closed | confirm the non-live test suite does not inherit `live` through `.env`   |

**diagnostic details**

01. (closed) `.env` line 7 changed to `MCF_MODE=live`. `easymcf/config.py`'s own field default (`os.environ.get("MCF_MODE", "fixture")`) is unchanged, so any process that never loads this `.env` still gets `fixture`.
02. (closed) `tests/conftest.py` already scrubs several `.env`-sourced keys immediately after the `initdb`/`resetdb` imports that trigger `load_dotenv()`, for the documented reason that a developer's personal `.env` must never change test behavior — `MCF_MODE` was not in that list. A standalone reproduction confirmed the gap directly: setting `MCF_MODE=live` in the process environment before importing `tests.conftest` still left `Config().mcf_mode == 'live'` once import completed. Added `MCF_MODE` to the same scrub block. Re-running the same reproduction after the fix showed `MCF_MODE` absent from `os.environ` post-import and `Config().mcf_mode == 'fixture'`. The full non-live suite then ran clean with `.env`'s `MCF_MODE=live` in place the whole time, confirming the fix holds under the real test run, not just the standalone repro.

_17.IS.12 (closed) MCF nav icon uses green instead of the maroon icon background_

**problem description**

The nav icon used the generic green control treatment rather than MCF's maroon magnifying-glass icon. Green is reserved for the active-session indicator.

**exception**

```log
<no runtime exception - direct visual review of the local MCF nav control>
```

**triggering actions**

01. sign in locally and inspect the MCF control in the nav bar.

**hypothesis**

The control background and its magnifying-glass mark are independent from the session indicator, so the button can use MCF maroon while the active indicator remains green.

**diagnostic steps**

| id          | seq | status | step                                      |
| ----------- | --- | ------ | ----------------------------------------- |
| 17.IS.12.01 | 01  | closed | inspect the icon and indicator color roles |
| 17.IS.12.02 | 02  | closed | apply the maroon MCF icon treatment        |

**diagnostic details**

01. (closed) The button's generic green styling and the SVG's own stroke were independent of the green, amber, and red session indicator classes.
02. (closed) The icon uses `assets/mcf-icon-magnifying-glass.png`, MCF's maroon magnifying-glass mark. CSS scales it to 2.94rem inside the fixed 2.4rem, overflow-clipped control so the source image's white square margin does not show. The active session remains the adjacent green dot, and the fixture E2E flow asserts the image path and crop sizing.

_17.IS.13 (closed) MCF indicator appears inside the icon instead of beside it_

**problem description**

The status dot was absolutely positioned in the icon button, visually competing with the MCF mark.

**exception**

```log
<no runtime exception - direct visual review of the local MCF nav control>
```

**triggering actions**

01. sign in locally and inspect the MCF control with each session state.

**hypothesis**

Making the button and status dot adjacent flex siblings keeps the icon clickable while making the indicator distinct from the brand mark.

**diagnostic steps**

| id          | seq | status | step                                        |
| ----------- | --- | ------ | ------------------------------------------- |
| 17.IS.13.01 | 01  | closed | inspect the icon and dot's positioning rule |
| 17.IS.13.02 | 02  | closed | make the dot a sibling beside the button    |

**diagnostic details**

01. (closed) `.mcf-status-dot` used absolute corner positioning inside `.mcf-nav-icon`.
02. (closed) Wrapped the button and dot in `.mcf-nav-control` and laid them out with inline flex. Existing UI checks retain the status-dot selector and pass for green, red, and amber states.

_17.IS.14 (closed) MCF status pop-up disappears before its connected state is usable_

**problem description**

The popup closed automatically when confirmation completed, hiding the connected account and Disconnect action before the user could review them.

**exception**

```log
<no runtime exception - observed immediately after confirming a fixture connection>
```

**triggering actions**

01. start a connection, confirm the displayed account, and observe the popup disappear.

**hypothesis**

The service has two independent auto-close paths, one in `confirm()` and one in the poll refresh, so removing both retains the modal until an explicit close action.

**diagnostic steps**

| id          | seq | status | step                                      |
| ----------- | --- | ------ | ----------------------------------------- |
| 17.IS.14.01 | 01  | closed | trace state changes after confirmation    |
| 17.IS.14.02 | 02  | closed | remove automatic successful-close actions |
| 17.IS.14.03 | 03  | closed | make the nav icon toggle the popup        |

**diagnostic details**

01. (closed) `confirm(true)` set `state.open` to false, and the next `mcf_attempt` poll repeated the same close when it saw `connected`.
02. (closed) Removed both assignments. The modal now renders the connected account and Disconnect action until its X control closes it.
03. (closed) Added `togglePopup()` and elevated the nav above the modal overlay. The fixture E2E test verifies icon open, icon close, icon reopen, confirmation retaining the connected state, and explicit close.

_17.IS.15 (closed) Singpass QR expires before the user receives it_

**problem description**

The live UI displayed Singpass's expired QR state within seconds of starting a fresh MCF Login. The QR itself was still a real Singpass element, but the runner did not reliably adopt the Login page that owns the transaction before it captured and presented the image.

**exception**

```log
<no Python exception - the live run remained awaiting approval with an expired QR>
```

**triggering actions**

01. start a fresh live MCF Login and scan the QR as soon as the modal renders.

**hypothesis**

MCF may open Login in a popup. The production runner must retain that popup as its owned page and capture the visible QR element from it immediately, exactly as the probe does; later bounded refreshes remain the fallback for a genuine rotation.

**diagnostic steps**

| id          | seq | status | step                                          |
| ----------- | --- | ------ | --------------------------------------------- |
| 17.IS.15.01 | 01  | closed | compare probe and runner Login page ownership |
| 17.IS.15.02 | 02  | closed | retain the Login popup and capture its QR     |
| 17.IS.15.03 | 03  | closed | keep bounded replacement refresh coverage     |

**diagnostic details**

01. (closed) The probe uses `context.expect_page()` around Login and continues with the page it receives. The production runner previously continued with its original MCF page even if Login opened a popup.
02. (closed) The runner now adopts the popup as its current page and captures the visible QR element from that page before it starts the approval wait. Fixture routing moved from the original page to the browser context so popup navigation cannot escape the fixture corpus.
03. (closed) `tests/backend/test_singpass_browser.py` passes 7 of 7 and `tests/backend/test_mcf_attempt.py` plus `tests/e2e/test_mcf_connection_flow.py` pass 15 of 15 in fixture mode. The refresh handoff remains covered.

_17.IS.19 (closed) active authenticated browser closes without clicking MCF Logout_

**problem description**

Cancelling an in-progress attempt closed its authenticated MCF browser context without first using MCF's own Logout control. The next action in that same context could therefore observe the retained browser session instead of a clean login state.

**exception**

```log
<no Python exception - session contamination risk identified from the disconnect lifecycle>
```

**triggering actions**

01. authenticate MCF, leave the browser active while awaiting confirmation, then cancel the attempt.

**hypothesis**

The request thread already signals the browser-owning thread through its control object. A logout flag can use that same handoff so the owning thread clicks MCF Logout before it closes the active context, without crossing Playwright's thread boundary or launching another browser.

**diagnostic steps**

| id          | seq | status | step                                          |
| ----------- | --- | ------ | --------------------------------------------- |
| 17.IS.19.01 | 01  | closed | inspect connected and active browser lifetimes|
| 17.IS.19.02 | 02  | closed | add logout to the browser-owner cancellation  |
| 17.IS.19.03 | 03  | closed | exercise fixture logout and cancellation      |

**diagnostic details**

01. (closed) The browser is already closed once confirmation writes `mcf_session`, so Disconnect must not launch another browser. Before that terminal point, the background thread still owns the authenticated context and cancellation only called `close()`.
02. (closed) `cancel()` now requests logout for an active `account_confirmation_required` attempt. The owning thread opens MCF's account menu, clicks Logout, then closes the context; saved state cleanup remains unchanged.
03. (closed) `test_cancel_authenticated_browser_logs_out_before_closing` verifies the coordinator signal, and `test_authenticated_browser_logs_out_in_its_own_context` verifies the fixture browser returns to the MCF home page. Both pass in the focused fixture suite.

_17.IS.20 (closed) Login popup bypasses the browser page used for QR capture_

**problem description**

MCF Login can open a new page. The probe adopted that page, but the production browser runner did not, leaving its QR and callback handling tied to the originating page instead of the page that owns the Singpass transaction.

**exception**

```log
<no Python exception - live verification showed the same user-visible QR failure despite fixture checks>
```

**triggering actions**

01. start MCF Login where the site opens its Singpass flow in a popup.

**hypothesis**

The probe's successful result depends on using the page returned by `context.expect_page()`. Reusing the original MCF page can show a stale or unrelated state and cannot reliably observe the callback that returns to the popup.

**diagnostic steps**

| id          | seq | status | step                                      |
| ----------- | --- | ------ | ----------------------------------------- |
| 17.IS.20.01 | 01  | closed | compare popup handling with the probe     |
| 17.IS.20.02 | 02  | closed | adopt the Login popup as the runner page  |
| 17.IS.20.03 | 03  | closed | route fixture popups through the context  |

**diagnostic details**

01. (closed) The probe binds `current` to the page returned by `context.expect_page()`. The runner only clicked Login on its existing page.
02. (closed) `open_qr_login()` now waits briefly for a popup, stores it when present, and retains same-tab navigation when no popup appears. QR extraction and callback waiting use that selected page.
03. (closed) `FixtureSingpassBrowser` now routes at the browser-context scope rather than only the first page. The popup Login test remains fixture-only and proves the QR path does not reach a live hostname.

_17.IS.21 (closed) confirmed MCF user is prompted for confirmation again_

**problem description**

Reconnecting the same MCF account after Disconnect reached `account_confirmation_required` and prompted the user to confirm an identity already stored in `mcf_session.confirmed_account_email`. A matching account should refresh the authenticated storage state and connect directly. First connections still require confirmation, and changed accounts remain blocked.

**exception**

```log
<no Python exception - the coordinator entered the wrong valid state for a previously confirmed matching account>
```

**triggering actions**

01. connect and confirm an MCF account, disconnect it, then reconnect with the same MCF account.

**hypothesis**

`_check_account_match()` returns `None` for both a first connection and a matching previously confirmed account. `_run_attempt()` therefore sends both cases to `account_confirmation_required`. The coordinator needs a distinct matching-account branch that exports the fresh storage state and writes `connected` immediately.

**diagnostic steps**

| id          | seq | status | step                                              |
| ----------- | --- | ------ | ------------------------------------------------- |
| 17.IS.21.01 | 01  | closed | trace the matching account after email extraction |
| 17.IS.21.02 | 02  | closed | auto-connect the previously confirmed account     |
| 17.IS.21.03 | 03  | closed | test first, matching, and changed account paths    |
| 17.IS.21.04 | 04  | closed | repeat disconnect and reconnect against live MCF   |

**diagnostic details**

01. (closed) The live reconnect extracted the same email stored in `mcf_session.confirmed_account_email`, then still rendered the Confirm and Reject controls. The mismatch guard returned no error, but the next unconditional transition was `account_confirmation_required`.
02. (closed) `_run_attempt()` now checks the stored confirmed email after the mismatch guard. An exact match exports the fresh browser storage state, closes the browser, updates `mcf_session`, and writes `mcf_attempt.status='connected'` without waiting for another user decision. A missing prior email still reaches confirmation, and a different email still reaches `interaction_required` with `account_mismatch`.
03. (closed) `test_previously_confirmed_account_reconnects_without_confirmation` covers the matching branch. The focused coordinator and E2E suite passed 18 of 18, including the existing first-connection confirmation and changed-account rejection coverage.
04. (closed) The human-gated live retest disconnected the confirmed account and reconnected the same account after the updated server restart. The flow moved through callback verification to connected without showing the account-confirmation prompt.

_17.IS.23 (closed) closing envcheck finds the earlier live server on port 5000_

**problem description**

The first full automated closing command for task 17.17 stopped in `scripts/envcheck.py` before pytest because port 5000 was already occupied.

**exception**

```log
[FAIL] port free — port 5000 already in use — kill the leftover process, or set PORT
```

**triggering actions**

01. run `MCF_MODE=fixture env/bin/python scripts/envcheck.py && MCF_MODE=fixture env/bin/python -m pytest` while the human-gated live validation server from the preceding MCF session work is still running.

**hypothesis**

The known live EasyMCF process still owns port 5000. Stopping that process and rerunning the identical command should make envcheck pass and allow pytest to start.

**diagnostic steps**

| id          | seq | status  | step                                  |
| ----------- | --- | ------- | ------------------------------------- |
| 17.IS.23.01 | 01  | closed  | stop the known live EasyMCF process   |
| 17.IS.23.02 | 02  | closed  | rerun the identical closing command   |
| 17.IS.23.03 | 03  | closed  | classify any subsequent first failure |

**diagnostic details**

01. (closed) Stopped the known live EasyMCF terminal. No unknown process or application defect was involved.
02. (closed) The identical command then printed `all checks passed` from envcheck and started all 401 pytest cases normally.
03. (closed) No subsequent first failure appeared. The replacement closing command completed with envcheck passing and all 401 pytest cases passing in 329.64 seconds, confirming this issue was limited to the expected port collision.

_17.IS.24 (closed) headed browser restores saved state but MCF still shows Login_

**problem description**

The live session-open request returned `202` and opened visible Chromium at MCF, but the page showed **Login** instead of the authenticated account control.

**exception**

```log
POST /api/v1/mcf_session/open HTTP/1.1 202
<visible MCF page shows Login; no Python exception>
```

**triggering actions**

01. open the connected MCF status pop-up with the persisted session and click **Open**.

**hypothesis**

The saved Playwright state omits browser storage MCF needs in a new context. The current export includes cookies and local storage, but does not request IndexedDB and Playwright storage state never includes sessionStorage. A stale server-side MCF session is the competing hypothesis.

**diagnostic steps**

| id          | seq | status  | step                                          |
| ----------- | --- | ------- | --------------------------------------------- |
| 17.IS.24.01 | 01  | closed  | inspect supported Playwright state categories |
| 17.IS.24.02 | 02  | closed  | persist IndexedDB and MCF sessionStorage       |
| 17.IS.24.03 | 03  | closed  | reconnect and export the expanded state        |
| 17.IS.24.04 | 04  | closed  | retry both headed-browser entry points         |

**diagnostic details**

01. (closed) Installed Playwright supports `storage_state(indexed_db=True)`. The current exporter does not enable it. Playwright's storage-state format does not include sessionStorage, so that state needs a protected sidecar restored through an init script. The existing saved file was not read or printed.
02. (closed) Storage export now includes IndexedDB and writes MCF sessionStorage to a separate `0600` sidecar. The headed launcher restores both before navigation.
03. (closed) A fresh live QR authentication exported both files and the restored page showed the authenticated dashboard.
04. (closed) The user confirmed authenticated MCF home and the exact lead posting both opened correctly.

_17.IS.25 (closed) UI remains connected after restored MCF session proves invalid_

**problem description**

After the visible MCF page showed Login, EasyMCF continued showing a green indicator and `Connected as ...` because the database row remained `mcf_session.status='valid'`.

**exception**

```log
<no Python exception - remote authentication contradicted the persisted valid status>
```

**triggering actions**

01. launch the saved session through **Open**, observe MCF Login, then reopen the EasyMCF status pop-up.

**hypothesis**

The interactive launcher schedules Chromium and returns without checking the initial MCF page. It therefore has no path to call the existing reauthentication transition when MCF rejects restored state. Initial-page verification should mark `mcf_session` expired and move the latest attempt to `reauthentication_required`. Local startup can also reject a `valid` row whose referenced file is absent, but remote validity cannot be known without an MCF navigation.

**diagnostic steps**

| id          | seq | status  | step                                         |
| ----------- | --- | ------- | -------------------------------------------- |
| 17.IS.25.01 | 01  | closed  | verify account control after initial load    |
| 17.IS.25.02 | 02  | closed  | mark rejected restored session expired       |
| 17.IS.25.03 | 03  | closed  | reconcile locally missing state at startup   |
| 17.IS.25.04 | 04  | closed  | confirm UI changes from green to reconnect   |

**diagnostic details**

01. (closed) Initial headed navigation checks the authenticated account control and Login control. Login is the authoritative contradiction.
02. (closed) A rejected restore marks `mcf_session` expired and the latest attempt `reauthentication_required`.
03. (closed) Startup and each session-status read downgrade a locally impossible valid row whose referenced file is absent.
04. (closed) The UI poll reads the corrected row and changes from green connected state to reconnect state.

_17.IS.26 (closed) fresh authentication cannot read the MCF account email_

**problem description**

The fresh QR callback reached the authenticated dashboard and opened the account menu, but the bounded email reads ended with no match. The attempt landed on `failed` after authentication succeeded and before expanded browser state could be exported.

**exception**

```log
RuntimeError: no account email found on the profile page
```

**triggering actions**

01. approve a fresh QR login after the old saved session is marked expired.

**hypothesis**

`_wait_for_visible_email()` waits for a text locator before reading the body. MCF can render an email across nested nodes that the text locator does not match even when the combined rendered body text contains it. Polling the rendered body text directly matches the successful probe behavior.

**diagnostic steps**

| id          | seq | status  | step                                       |
| ----------- | --- | ------- | ------------------------------------------ |
| 17.IS.26.01 | 01  | closed  | poll rendered body text for the email      |
| 17.IS.26.02 | 02  | closed  | rerun fixture extraction coverage          |
| 17.IS.26.03 | 03  | closed  | authenticate live and export expanded state |

**diagnostic details**

01. (closed) Email readiness now polls rendered body text directly after the account-menu click.
02. (closed) Focused browser extraction and coordinator coverage passes.
03. (closed) Fresh live authentication extracted the confirmed email and exported expanded state.

_17.IS.27 (closed) user closing headed browser logs `TargetClosedError` as failure_

**problem description**

Closing the user-owned headed browser ended its page while the passive owner thread was inside `wait_for_timeout(250)`. The generic exception handler logged this expected closure as a launcher failure.

**exception**

```log
playwright._impl._errors.TargetClosedError: Page.wait_for_timeout: Target page, context or browser has been closed
```

**triggering actions**

01. close the headed Chromium window after launch.

**hypothesis**

`TargetClosedError` is the normal completion signal for the user-owned lifecycle. Catching it inside the passive loop should exit quietly while preserving logging for genuine launch, restore, and navigation failures.

**diagnostic steps**

| id          | seq | status  | step                                      |
| ----------- | --- | ------- | ----------------------------------------- |
| 17.IS.27.01 | 01  | closed  | catch target closure in the passive loop  |
| 17.IS.27.02 | 02  | closed  | rerun focused browser-open coverage       |

_17.IS.28 (closed) headed launcher imports unavailable public `TargetClosedError`_

**problem description**

Fresh authentication, account extraction, and expanded state export succeeded. Each **Open** request returned `202`, but its owner thread exited before launching Chromium because the local import named an exception Playwright does not expose from the public sync API.

**exception**

```log
ImportError: cannot import name 'TargetClosedError' from 'playwright.sync_api'
```

**triggering actions**

01. click **Open** after fresh authentication writes the expanded storage-state files.

**hypothesis**

The installed Playwright exposes the public base `Error` from `playwright.sync_api`, while `TargetClosedError` is internal. Catching the public error around the passive wait and accepting it only when the selected page is closed preserves expected closure handling without a private import.

**diagnostic steps**

| id          | seq | status  | step                                       |
| ----------- | --- | ------- | ------------------------------------------ |
| 17.IS.28.01 | 01  | closed  | replace unavailable exception import       |
| 17.IS.28.02 | 02  | closed  | test public import and session-open contract |
| 17.IS.28.03 | 03  | closed  | restart and retry Open with expanded state |

**diagnostic details**

01. (closed) `_run_interactive_browser()` now imports the public `playwright.sync_api.Error` and treats it as expected only when the selected page reports closed.
02. (closed) The focused browser, session-open, and coordinator suite passes 30 of 30.
03. (closed) After restart, popup Open and an exact lead posting both launched authenticated headed Chromium. User closure ended quietly.

_17.IS.29 (closed) seed claims authentication without seeding browser state_

**problem description**

The seed wrote `mcf_session.status='valid'` and `cookie_ref='mcf_session_1.json'` while deliberately creating no browser-state file. A fresh reset could therefore display a connected session that never existed.

**resolution**

Seed generation now writes `status='missing'`, `uploaded_at=NULL`, and `cookie_ref=NULL` for every account. It may retain a confirmed account identity as history. Tests that require a connected session create both the valid row and its state file explicitly. The standalone UI regression proves a reseeded user renders red with Connect and without Open or Disconnect.

_17.IS.30 (closed) status poll reports valid when authenticated Open must fall back_

**problem description**

The frontend status poll trusted the database status alone. The Open endpoint also required a safe `cookie_ref` and an existing state file. Those two authorities could disagree, producing a green connected UI followed by ordinary unauthenticated navigation.

**resolution**

Startup and every `mcf_session` read now reconcile valid rows against the same local file requirement used by Open in every mode. Open immediately downgrades an impossible valid row before returning redirect fallback. Focused backend and browser coverage passes 45 of 45, including startup, post-start file loss, authenticated opening, and native fallback.

_17.IS.31 (closed) restart script launches the fixture Singpass flow_

**problem description**

Running `scripts/restart.sh --reset-seed` could start with the wrong effective `MCF_MODE`. The resulting fixture QR lacked Singpass's red “i” and its fixture page redirected itself to the authenticated dashboard after eight seconds. This produced `Connected as <INITIAL_USER_EMAIL>` without a real Singpass approval.

**root cause**

The successful diagnostic run supplied `MCF_MODE=live` directly on the launch command. The restart helper delegated mode loading to application startup, so the process environment and dotenv precedence could select a different value. The two launch paths were not equivalent.

**resolution**

`scripts/restart.sh` now parses `MCF_MODE` directly from `.env`, accepts only `live` or `fixture`, exports that exact value, and reports it in the startup line. `Config` independently rejects all other values. The exact command `scripts/restart.sh --reset-seed` launched PID `262455` with `MCF_MODE=live`, seeded user 1 as `missing` with no `cookie_ref`, and displayed the real Singpass QR. The user confirmed the connection flow works. The focused Feature 17 suite passes 41 of 41, and the closing environment check plus full fixture suite passes 408 of 408.

**user actions**

- any interactive Singpass login, and any live-tier check against the real MCF origin, per `ARCH-TEST-06` and `CLAUDE.md`'s boundary against unattended live-site access

## Tasks Details

### 17.17 (closed) authenticated browser for user

#### Tasks

| id       | seq | status  | subtask                                  | issue    |
| -------- | --- | ------- | ---------------------------------------- | -------- |
| 17.17.01 | 01  | closed  | define scope and closure                 | 17.IS.22 |
| 17.17.02 | 02  | closed  | test strategy and test cases             | -------- |
| 17.17.03 | 03  | closed  | design                                   | -------- |
| 17.17.04 | 04  | closed  | edit locations                           | -------- |
| 17.17.05 | 05  | closed  | implement authenticated browser endpoint | -------- |
| 17.17.06 | 06  | closed  | implement reusable MCF link behavior     | -------- |
| 17.17.07 | 07  | closed  | validate live headed browser             | -------- |
| 17.17.08 | 08  | closed  | close sub-feature and issue              | 17.IS.22 |

#### Scope

Task 17.17 lets the user open MyCareersFuture in a separate visible Chromium window restored from EasyMCF's saved authenticated session. The connected-state pop-up gains an **Open** command for the MCF home page. Current and future links to HTTPS `mycareersfuture.gov.sg` URLs can adopt the same behavior through one reusable frontend directive. Current consumers are lead posting links. Future consumers include apply-repair links.

The feature has two paths.

01. **authenticated path.** When the signed-in EasyMCF user has `mcf_session.status='valid'`, a `cookie_ref`, and the referenced storage-state file, the backend launches `headless=False` Chromium in a dedicated thread, restores that storage state, and navigates the first page to the requested MCF URL.
02. **plain-link fallback.** When no valid saved session is available, the link keeps ordinary `target="_blank"` behavior. If frontend state says valid but the backend finds missing session material, the endpoint returns `mode='redirect'` and the frontend opens the requested URL normally.

The browser is handed to the user after initial navigation. EasyMCF does not inspect page content, restrict navigation, synchronize later activity, perform actions, or close the window. The owning background thread keeps the Playwright transport alive because stopping Playwright would terminate Chromium. It exits after the user closes the final window.

In scope:

01. an **Open** button in the connected MCF status pop-up.
02. `POST /api/v1/mcf_session/open`, accepting the requested MCF URL.
03. headed Chromium restored from the requesting user's saved storage state.
04. HTTPS MCF-domain validation before authenticated browser launch.
05. reusable behavior for current lead-list and Lead Detail MCF links.
06. ordinary new-tab fallback when the session is unavailable.
07. a reusable frontend contract for future apply-repair links.
08. user ownership through the signed-in EasyMCF session.

Out of scope:

01. controlling, observing, or recording what the user does after initial navigation.
02. restricting the headed browser to MCF after launch. The user may navigate anywhere.
03. exporting browser changes back into `mcf_session.cookie_ref` when the user closes it.
04. automatically refreshing an expired MCF session from the headed browser.
05. opening arbitrary non-MCF targets with authenticated storage state.
06. deduplicating windows or keeping an interactive-window registry.
07. implementing future apply-repair screens. They consume `mcf-link` when built.

**closure**

- **17.17.CK.01** the connected MCF pop-up shows **Open**, which launches MCF home in visible Chromium restored from the requesting user's valid saved session.
- **17.17.CK.02** MCF links on the Leads list and Lead Detail use the authenticated browser while the saved session is valid.
- **17.17.CK.03** an absent, non-valid, or file-missing session falls back to an ordinary new-tab URL without headed Chromium.
- **17.17.CK.04** only HTTPS `mycareersfuture.gov.sg` URLs and subdomains can receive authenticated browser state. Other targets answer `400` on `url`.
- **17.17.CK.05** the request thread owns no Playwright object. One daemon thread creates and retains the headed browser until the user closes its final window.
- **17.17.CK.06** EasyMCF performs no page action after initial navigation and places no navigation restriction on the user-owned window.
- **17.17.CK.07** future MCF links can adopt the behavior with `mcf-link` and no feature-specific controller code.
- **17.17.CK.08** connection, mismatch, disconnect, and plain-link behavior remain unchanged.

#### Phases

01. define the endpoint contract, browser handoff boundary, reusable link behavior, and checks.
02. implement the backend launcher and test its branches without opening a GUI.
03. implement the pop-up command and link directive, then test interception and fallback.
04. restart the live app and perform the user-triggered headed-window checks.
05. record evidence, close `17.17`, and close `17.IS.22`.

#### Test Strategy

Task 17.17 uses four independent layers.

01. **backend oracle.** Flask test-client cases verify URL rejection, redirect fallback, and headed-browser scheduling while replacing the launcher with a recorder. No GUI opens and no MCF request occurs.
02. **mock end to end.** Playwright drives the real AngularJS and Flask stack. Route interception records authenticated-open requests and aborts external MCF navigation.
03. **regression.** The existing MCF coordinator and E2E suites cover connection, mismatch, cancellation, session status, and disconnect behavior.
04. **human-gated live validation.** The user clicks **Open** with the real session, confirms a visible authenticated browser, navigates independently, and closes it.

**isolation**

Automated cases use `MCF_MODE=fixture`, a temporary database, and a temporary secrets directory. Backend tests replace the headed launcher. E2E tests intercept target MCF requests before hostname resolution. Only the explicit human-gated check uses the live saved session.

#### Test Cases

| id          | scope-item  | layer    | check                                             |
| ----------- | ----------- | -------- | ------------------------------------------------- |
| 17.17.TC.01 | 17.17.CK.04 | oracle   | invalid target returns `400` on `url`             |
| 17.17.TC.02 | 17.17.CK.03 | oracle   | missing session returns `mode='redirect'`         |
| 17.17.TC.03 | 17.17.CK.01 | oracle   | valid saved state schedules headed browser        |
| 17.17.TC.04 | 17.17.CK.05 | oracle   | launcher receives saved-state path and target     |
| 17.17.TC.05 | 17.17.CK.01 | mock e2e | pop-up Open posts MCF home                        |
| 17.17.TC.06 | 17.17.CK.02 | mock e2e | valid-session lead link posts its exact MCF URL   |
| 17.17.TC.07 | 17.17.CK.03 | mock e2e | missing-session lead link opens its native target |
| 17.17.TC.08 | 17.17.CK.07 | inspect  | lead links use the shared `mcf-link` directive    |
| 17.17.TC.09 | 17.17.CK.08 | regress  | existing MCF backend and E2E suites pass          |
| 17.17.TC.10 | 17.17.CK.01 | human    | Open launches visible authenticated MCF home      |
| 17.17.TC.11 | 17.17.CK.06 | human    | free navigation and user close need no app action |

Automated command:

```bash
MCF_MODE=fixture env/bin/python -m pytest -q tests/backend/test_mcf_session_open.py tests/backend/test_mcf_attempt.py tests/e2e/test_mcf_connection_flow.py
```

#### Design

##### API contract and authorization

`POST /api/v1/mcf_session/open` accepts `{"url": "https://www.mycareersfuture.gov.sg/..."}`. Authentication comes from the existing request guard and `g.user_id`. The service reads only that user's `mcf_session` row. The request cannot supply a user id, `cookie_ref`, path, browser option, or storage-state payload.

The response is `202 {"mode": "authenticated", "url": ...}` when a headed browser is scheduled, or `200 {"mode": "redirect", "url": ...}` when the caller should use an ordinary URL. Invalid targets use the shared validation shape, `400` with `field: "url"`.

The URL validator uses structured parsing. It requires `https`, and the hostname must equal `mycareersfuture.gov.sg` or end with `.mycareersfuture.gov.sg`. This excludes lookalike suffixes and prevents authenticated state from being deliberately bootstrapped onto another origin.

##### Saved-state selection

The service requires `status='valid'`, a non-empty `cookie_ref`, and an existing file under `Config.secrets_dir`. `cookie_ref` must equal its basename, preventing path traversal outside the secrets directory. A failed condition returns plain-link mode.

**implementation decision.** A missing saved-state file uses fallback instead of a hard API error. The requested MCF page remains useful without authentication, and the feature explicitly requires fallback. Session expiry remains owned by apply automation and the reconnect flow.

##### Browser bootstrap and ownership

The request thread starts one daemon thread and returns. That thread starts Playwright, launches Chromium with `headless=False` and `channel="chromium"`, creates a context from the resolved storage-state path, opens one page, and navigates to the validated target with `wait_until="domcontentloaded"`.

After initial navigation, the thread does not read content or perform actions. It calls only a short Playwright wait while at least one page remains, keeping the transport and Chromium process alive. The user owns navigation and interaction. Closing the last page ends the loop and releases the process.

**implementation decision.** Chromium cannot be detached by ending Playwright because that also terminates the launched process. The passive owner thread is lifecycle scaffolding. It sends no behavioral command after initial navigation.

**deviation.** Connection and apply automation use headless contexts owned by automated runs. Task 17.17 deliberately uses a headed context handed to the user, while retaining the one-thread-per-Playwright-owner rule established by `17.IS.04`.

##### Frontend link behavior

`McfConnect` owns the common decision. `hasValidSession()` reads the session already maintained by its poll. `shouldOpenAuthenticated(url)` requires a valid session and an HTTPS MCF URL. `openUrl(url)` calls the named endpoint and opens a normal new tab only when the endpoint returns `mode='redirect'` or fails.

The `mcfLink` attribute directive leaves a click untouched unless `shouldOpenAuthenticated()` is true, preserving native `target="_blank"` fallback. On the authenticated path it prevents native navigation and delegates the exact rendered `href` to `McfConnect.openUrl()`.

Both Leads layouts and Lead Detail **Open post** use `mcf-link`. Future apply-repair links use the same attribute. Non-MCF links remain native even with a valid MCF session.

The connected-state pop-up uses a command button because it opens MCF home rather than representing inline document navigation. It calls `McfConnect.openUrl('https://www.mycareersfuture.gov.sg/')` and renders only for a valid session.

##### Failure behavior and observability

Validation and authentication errors use the existing API surface. Failures after the endpoint returns, including Chromium launch or local-display failure, use `logger.exception` with the prefix `MCF_DIAG interactive browser failed`. No storage-state content, cookie, email, or secret is logged.

The endpoint mutates neither `mcf_session` nor `mcf_attempt`. User activity in the headed browser does not change EasyMCF records. A later apply run remains responsible for determining whether the saved session still authenticates.

#### Update Locations Other Docs

| id | location                    | update                                      |
| -- | --------------------------- | ------------------------------------------- |
| 01 | **api design**              | named session-open endpoint                 |
| 02 | **user interface**          | connected Open and authenticated MCF links  |
| 03 | **frontend app**            | `mcfLink` directive and shared URL behavior |
| 04 | **architecture**            | headed user-owned browser handoff           |
| 05 | **apply automation tracker**| repair links consume `mcf-link`             |

These updates remain part of `17.17.07` and are completed after the live behavior confirms the design. No requirements or data-model change is expected.

#### Edit Locations

| id          | path                                      | change                    |
| ----------- | ----------------------------------------- | ------------------------- |
| 17.17.EL.01 | `services/mcf_connection.py`              | validate and launch       |
| 17.17.EL.02 | `api/mcf_session_routes.py`               | named Open endpoint       |
| 17.17.EL.03 | `easymcf/__init__.py`                     | register blueprint        |
| 17.17.EL.04 | `core/api-client.service.js`              | endpoint method           |
| 17.17.EL.05 | `core/mcf-connect.service.js`             | shared URL decision       |
| 17.17.EL.06 | `shared/mcf-link/`                        | reusable directive        |
| 17.17.EL.07 | `shared/mcf-connect-modal/`               | connected Open command    |
| 17.17.EL.08 | `leads/{leads,lead-detail}.html`          | adopt `mcf-link`          |
| 17.17.EL.09 | `frontend/index.html`                     | load directive            |
| 17.17.EL.10 | `tests/backend/test_mcf_session_open.py`  | endpoint oracles          |
| 17.17.EL.11 | `tests/e2e/test_mcf_connection_flow.py`   | both frontend paths       |
| 17.17.EL.12 | `010.17-mcf-website-session.md`           | sub-feature record        |

Paths under `core`, `shared`, and `leads` are relative to `frontend/app`. No schema, seed, configuration, or saved-state format change is expected. Future apply-repair screens are consumers rather than edit locations.

#### Implement

##### 1. Backend authenticated-window contract

edit locations: `17.17.EL.01` to `17.17.EL.03`, `17.17.EL.10`

Implemented the endpoint, MCF URL validation, per-user session and file checks, daemon-thread launch, headed storage-state context, passive window lifetime, and redirect response. The focused backend suite passes 17 of 17 with the launcher replaced by a recorder.

##### 2. Reusable frontend behavior

edit locations: `17.17.EL.04` to `17.17.EL.09`, `17.17.EL.11`

Implemented the Open command, shared URL decision, `mcfLink` directive, current lead link adoption, and plain-link fallback. The MCF E2E and endpoint suites pass 9 of 9 with live MCF requests intercepted.

##### 3. Live headed-window validation

edit locations: none

Closed. After fresh authentication exported IndexedDB and sessionStorage, the user confirmed that pop-up **Open** launched an authenticated MCF dashboard and a lead title launched the exact authenticated posting. The user navigated and closed the windows without EasyMCF intervention. After disconnect removed both saved-state files, the user confirmed the same lead title opened as an ordinary unauthenticated browser URL.

#### Validate

Automated focused validation passes. The earlier full fixture-mode closing command reported envcheck passing and 401 pytest cases passing in 329.64 seconds. Live headed-window validation passed for MCF home and an exact lead posting. Disconnect removed both state files, and the ordinary unauthenticated URL fallback passed by user confirmation. The seed and false-positive regressions added afterward pass in the focused 45-case slice. Task `17.17` and issue `17.IS.22` are closed.

**user actions**

- completed: authenticated MCF home, authenticated exact-post link, user-owned navigation and close, disconnect, and ordinary unauthenticated fallback.

## Guideline

### instructions

review and strictly follow these relevant skills when performing tasks for this feature implementation and working with this document

### relevant skills

- markdown-tables
- feature-implementation-guide
